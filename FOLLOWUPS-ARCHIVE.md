# Followups — archive

Closed entries, moved out of `FOLLOWUPS.md` so the live list shows only open
work. **Nothing here is summarised or truncated** — each entry keeps its full
original text and the note explaining how it closed, because the closing note is
usually the part worth reading later (several of these record a root cause that
turned out to be different from the one first written down).

This file is NOT read at session start. `FOLLOWUPS.md` and `BACKLOG.md` are.
Search here before re-investigating anything — it sits alongside `PROGRESS.txt`
in the past-work lookup order in `CLAUDE.md`.

Entries keep their `[x]` marker and their original priority, so a closed P0 still
reads as a P0. Ordering is the order they held in `FOLLOWUPS.md`, not the order
they were fixed.

## Archived 2026-08-23

- [x] P2 | 2026-08-23 | manifest.json:14 (hooks.claude-code.version) | STILL 1.1.0 WHILE THE SPEC IS 1.5.0. The desktop's outdated-hooks banner (updater.rs `check_remote_hooks`) compares the stamped `ymux_meta.hooks_version` against the MANIFEST, so until this moves nobody is ever told to re-run `setup-hooks` — and without that re-run the Phase 84.B red light never appears for anyone. Deliberately not bumped in the feature commit: that would banner every current 0.4.5 user before a CLI exists that can honour 1.5.0. **This belongs in the release checklist for whatever ships Phase 84.B** — add it to docs/RELEASING.md rather than leaving it here. **CLOSED 2026-08-23 — not dropped, relocated exactly as this entry asked. It is now blocker 0.4b in docs/RELEASE-0.5.0-CHECKLIST.md, and docs/RELEASING.md §4½ already carries the generic three-file procedure (bump `ymux_hooks_version` in hooks/<agent>.json → `BUNDLED_CLAUDE_VERSION` in cli/src/hooks.rs → `hooks.<agent>.version` in manifest.json). Current state confirmed in tree: spec and bundled constant are both 1.5.0, manifest is still 1.1.0 — correctly so until the cut. Nothing to do here.**
- [x] P2 | 2026-08-23 | repo root + app/src-tauri/resources/ | DONE — the four stray artifacts are gone and the checkout is clean. Root cause was NOT "never gitignored" as first written: `.gitignore` covers `ymux-*-test.exe` and `resources/ymux-cli.exe`, and the winmux->ymux rename simply left the pre-rename copies outside every pattern. Deleted (~195MB, Yossi's call): `winmux-DEBUG-test.exe` (2026-07-15 — superseded, the live debug build lives at `%USERPROFILE%\winmux-debug-test\winmux-DEBUG-test.exe` where `run-winmux-debug.bat` expects it), `winmux-rtl-test.exe` (2026-07-15 — NOT the build cited in the RTL P1 below, which names `ymux-rtl-test` at 2026-08-19 16:32) and `winmux-utf8-test.exe` (2026-08-03, unreferenced). KEPT, moved out of the repo rather than deleted: `app/src-tauri/resources/winmux-cli.exe` -> `%USERPROFILE%\winmux-debug-test\winmux-cli-PRERENAME.exe`. It is a genuine pre-rename CLI (built 2026-08-13, five days before the rename) and it is the artifact RELEASE-0.5.0-CHECKLIST 1.5 needs to prove the legacy pipe listener; rebuilding one later means checking out a pre-rename commit. No `.gitignore` change needed — nothing produces winmux-named artifacts any more.
- [x] P1 | 2026-08-20 | app/src/LocalSetupFlow.tsx + app/src-tauri/src/lib.rs + app/src-tauri/src/local_setup.rs | **THE macOS MERGE IS DEFERRED, NOT DONE — `origin/main` AND THE 0.5.0 BRANCH HAVE DIVERGED ARCHITECTURALLY.** v0.5.0-beta.1 was tagged from `claude/worktrees-clean-build-988849` deliberately, leaving `main` untouched, because merging `origin/main` (Phase 82, the macOS port, +3018 lines, PRs #12/#13) into the branch produces **29 conflict blocks across 14 files** that are architectural rather than textual: the branch DELETED the WSL wizard scaffolding, and main BUILT macOS ON TOP OF IT. Main reuses the WSL group as a generic local-setup group and never renamed the variable — `const MAC_CHAIN = ["InstallTmuxLocal","DeployTmuxConfLocal"]`, then `if (wslGroup() && r && mac) {...MAC_CHAIN} else if (wslGroup() && r) {...WSL_CHAIN}`, and `canStart = () => buildSteps().length > 0 || (mac && wslGroup())`. Deleting that group removes the ground macOS stands on. **THE FIX IS ALREADY DESIGNED — do not re-derive it.** It is symmetric, and the branch already supplies the Windows half (`InstallZellij` exists at app/src/LocalSetupFlow.tsx:45 and app/src-tauri/src/local_setup.rs:1219): rename `wslGroup` -> `localSetupGroup`, KEEP the group, and swap the contents — Windows gets `["InstallZellij"]`, macOS keeps `["InstallTmuxLocal","DeployTmuxConfLocal"]`, WSL is dropped. In lib.rs the branch's `tmux_name` -> `zellij_session` rename becomes a cfg split (zellij on Windows, tmux on macOS) — main added LOCAL tmux persistence for mac, so the field is load-bearing on that side. **WHEN RESOLVING, use `git merge-file --union` for the append-only files** (FOLLOWUPS.md, PROGRESS.txt, docs/DECISIONS.md, CLAUDE.md) — a Python resolver truncated two of them to zero bytes on 2026-08-20 and it was only caught by a line-count check. **CI TRIGGERS MATTER HERE:** ci-windows.yml is `workflow_dispatch` + `pull_request` only (a branch push runs nothing), and build-macos-intel.yml is `workflow_dispatch` + push to `macos-build` — it does NOT run on a PR, so the macOS half of this merge ships unverified unless it is dispatched by hand. Open a PR so both can be gated before anything reaches main. **DONE 2026-08-23** — merged, and the pre-designed fix held with two corrections found by reading the code rather than the note. (1) Windows does NOT get a group: `InstallZellij` is already an ordinary TOOL_ROW (LocalSetupFlow.tsx:55), so a `["InstallZellij"]` group would have offered the same install twice. `wslGroup` is kept under its old name and shown only when `mac` — Windows sees the workspace name/cwd block alone. (2) The CI warning is stale: Phase 82.E rewired build-macos-intel.yml, so BOTH it and ci-windows.yml now trigger on `pull_request: [main]` — a PR gates both halves with no manual dispatch. In lib.rs the split landed as predicted: one `persist_session: Option<String>` parameter, `cfg(windows)` zellij / `cfg(not(windows))` tmux for the attach injection, the default (`persistent.unwrap_or`) staying true on Windows and false elsewhere, and `KillTarget::Zellij` / `KillTarget::LocalUnix` as per-platform variants. Also: main and the branch each carried the same five FOLLOWUPS entries under the pre- and post-rename spelling (`~/.winmux/bin` vs `~/.ymux/bin`) — a blind union would have re-created the duplication `f98e079` removed.
- [x] P3 | 2026-08-23 | .github/workflows/build-macos-intel.yml | actions/checkout@v4, setup-node@v4 and upload-artifact@v4 all warn that Node 20 is deprecated and GitHub is already forcing them onto Node 24. Harmless today; bump to @v5 across all four workflows before GitHub drops the shim. **DONE 2026-08-23 (PR #19).** Went past @v5 to the newest majors — checkout/setup-node @v7, setup-go @v7, upload-artifact @v7, cache @v6 — each verified as `runs.using: node24` in its real action.yml. The bigger find was that this entry understated the problem: all four workflows also pinned `node-version: 20`, and Node 20 reached EOL on 2026-04-30, so CI was building the shipped frontend on an unsupported runtime. Now 24. Same PR added `permissions: contents: read` everywhere and a `.github/dependabot.yml` so the pins are watched from here on.
- [x] P0 | 2026-08-19 | app/src-tauri/src/tunnel_registry.rs (new), src/lib.rs (setup_workspace_reverse_tunnel / spawn_ssh / workspace_ensure_connected), cli/src/main.rs (rpc_call) | THE SUDDEN DISCONNECTS. FIXED by Phase 80.A-C, NOT VERIFIED LIVE. The original diagnosis in this entry was WRONG in its central claim and is corrected here rather than deleted, because the wrong version is the one someone will remember. It said the 20-vs-5 gap between `tcpip_forward got remote port` and `wrote ... last.env` was the cause. It is not: at 19:56:27 last.env was written CORRECTLY with 44495, and at 19:56:31 the same pane's hook still dialed 34173, the port from 73 minutes earlier. REAL ROOT CAUSE: a long-lived `claude` inside tmux inherits YMUX_SOCKET_ADDR at spawn and its hooks inherit it in turn; a reconnect allocates a fresh port; nothing can rewrite the environment of a running process; and cli/src/main.rs:1026 returns early from load_fallback_env_file whenever YMUX_SOCKET_ADDR is set, so on the one path where the file would help it is the one thing never read. Writing last.env more often fixes nothing — the port has to stop changing. FIXED BY: (a) sticky port — re-request the port the workspace last held, so the address baked into the running claude stays valid; desktop-only, works against every already-provisioned remote; (b) a per-workspace connect lock, ordered before bootstrap_guard::host_lock, so workspace_ensure_connected and spawn_ssh cannot allocate rival forwards (PaneView.smartConnect made that the NORMAL path — two tcpip_forwards 38ms apart, one watcher slot, loser orphaned, 4 server-side hygiene reaps); (c) TunnelLease, an RAII rollback for the four early exits that leaked a port + token + watcher each (three are `?` operators, hence RAII rather than manual); (d) TunnelRegistry replacing internal_reverse_tunnel_remote_ports (a HashSet sampled with .iter().next() that only ever grew — 20 dead ports) and workspace_tunnel_tokens (cleared nowhere in the codebase), so port/token/owning-session are one triple and try_ensure_port_watcher can no longer cross-pair them into -DENIED bad-mac; (e) the env write moved into setup_workspace_reverse_tunnel so every allocation updates it, with pane_id now Option and a read-modify-write that preserves the existing YMUX_PANE_ID — its ABSENCE makes the remote hook disable gating entirely, so blanking it would be worse than the original bug; (f) CLI-side, rpc_call retries once against a re-read last.env when its inherited address fails, bypassing the :1026 early-out. WHAT IS STILL OPEN: the whole thing is unverified against a real sshd. The specific unknowns are (1) whether sshd grants a re-requested specific port right after the previous listener closed — theoretically yes via SO_REUSEADDR on listen sockets, but Repro C settles it; (2) the russh trap that tcpip_forward returns Ok(0) for a NONZERO request, which request_reverse_forward is the single place that knows about — if that is wrong the tunnel dies silently with no error; (3) (f) is inert until every remote is re-bootstrapped with the new CLI. SMOKE (Repro C, the one that matters): connect a persistent tmux pane, start claude, let a hook succeed, note the port, force a reconnect WITHOUT touching the running claude, then type a prompt. Expect `reused sticky remote port <same port>` and no rpc-error-fallback. Invariant one-liner: `grep -c 'tcpip_forward got remote port'` must equal `grep -c 'wrote .*last\.env'`.
- [x] P2 | 2026-08-19 | app/src-tauri/crates/ymux-core/src/log_writer.rs (new), crates/ymux-core/src/lib.rs (write_line), src/log_sync.rs, cli/src/main.rs (hook_log) | TORN LOG LINES. FIXED by Phase 80.D, NOT VERIFIED LIVE. Root cause was exact: `writeln!` on an unbuffered std::fs::File goes through core::fmt::write, which issues the payload and the trailing "\n" as TWO write() syscalls; O_APPEND makes each atomic but not the pair, and there was no lock anywhere. Rotation was a stat-then-rename TOCTOU that could lose a whole 5MB generation, and on Windows a rename succeeds with our handle still open (FILE_SHARE_DELETE) so the writer kept appending into debug.log.1. log_sync amplified it by submitting up to 256KiB x 3 files x N hosts one line at a time. FIXED BY: a dedicated OS thread owning the handle, fed by a bounded std::sync::mpsc queue (8192, non-blocking, drops are reported as one counted WARN line); batches coalesced into a single write_all; rotation/prune/clear moved onto that thread and ordered against the lines around them; rotation closes the handle before renaming. Public signatures and the on-disk format are unchanged. cli hook_log got the same single-write_all fix — a separate process, so no queue is possible there, but log_sync copies its lines into debug.log verbatim. STILL OPEN: two ymux.exe instances would still race on rotation — tauri-plugin-single-instance is absent from app/src-tauri/Cargo.toml, see the separate entry below.
- [x] P2 | 2026-08-20 | app/src/copyBidi.ts + app/src/terminalInstance.ts (copyToClipboard) | **DONE 2026-08-20. Hebrew copied out of a pane pasted REVERSED.** Measured by Yossi, and the two panes were exactly inverted: plain PowerShell displayed reversed but pasted CORRECTLY (its buffer is logical), while Claude Code displayed correctly but pasted reversed (its buffer is visual, and `unicode-bidi: bidi-override` renders visual bytes verbatim, which is why the screen looked right). Fixed with a visual->logical pass on the way to the clipboard, gated per pane on the Claude-in-front signal, routed through one `copyToClipboard` that all four copy paths now share. **Two claims in the original entry were WRONG and are corrected here:** (1) it said `mouse_mode false` was 'the precondition that makes it reachable at all: with zellij owning the mouse it wrote to the clipboard over OSC52 and ymux never saw the text' — false, `ClipboardAddon` has always routed OSC 52 through our own provider, so the mouse setting was never a blocker and the wheel could come back for free; (2) it framed visual order as a property of local/ConPTY panes generally — it is a property of Claude Code, not of the shell. Residual, and NOT fixable: two different logical strings can share one visual form (`"שלום עולם 123 abc"` and `"שלום עולם abc 123"` both render to `"abc 123 םלוע םולש"`), so a selection with adjacent digits and Latin at an RTL edge picks one reading. Pinned as a test.
- [x] P3 | 2026-08-20 | FOLLOWUPS.md:33 and :38 | The same entry is filed twice — one spelling `winmux-tmux.conf`, the other `ymux-tmux.conf`. A leftover of the rename. Delete the stale one. **DONE 2026-08-23 — the duplicate is gone; a single tmux.conf entry survives (the `ymux-tmux.conf` spelling, the `set -g mouse on` vs `mouse off` contradiction). Lesson for future entries: line numbers in THIS file drift every time something is added or closed, so cite the entry's text, never `FOLLOWUPS.md:NN`.**

- [x] P1 | 2026-08-19 | app/src-tauri/cli/src/hooks.rs (is_ymux_entry / the skip path) | **`setup-hooks` decides an entry is 'already present' by NAME and never checks that the command still exists, so the winmux -> ymux rename left a permanently dead hook.** From Yossi's Claude pane, verbatim: `Stop hook error: /usr/bin/bash: line 1: C:\Users\mlastudent371\winmux-cli.exe: No such file or directory`. His ~/.claude/settings.json references BOTH spellings; `is_ymux_entry` correctly matches the old one as ours, and the installer then reports `already present (would skip)` and leaves the dead path in place. Verified here: `--dry-run` skips all four, `--force` rewrites all four with the live path and takes a timestamped .bak. Two costs, and the second is the expensive one: the hook fires and errors on EVERY Claude stop, and it silently disabled the hook-based half of the per-pane 'Claude is in front' signal that local RTL depends on -- which is why local hooks never reached the app all session while remote ones did, and why that looked like a detection bug for several rounds. Fix: in the skip path, resolve the entry's executable and treat a missing file as needing replacement, so 'already present' means 'present AND runnable'. Workaround until then: re-run with `--force`. **FIXED 2026-08-23 by Phase 84.B — `ymux_entry_is_runnable` in app/src-tauri/cli/src/hooks.rs resolves each ymux entry's executable token and treats a rooted path that is not on disk as needing replacement, so "already present" now means present AND runnable. Deliberately conservative: only a ROOTED path counts, because a bare command name resolves through PATH and a `${VAR}` resolves at run time — guessing wrong would rewrite a working hook on every run. `is_absolute() || has_root()` rather than `is_absolute()` alone, so a POSIX-style `/home/y/.winmux/bin/winmux` (not absolute on Windows, but rooted) written under WSL is still caught. Unit-tested. STILL NOT VERIFIED LIVE — the smoke step is 13b.8 in docs/RELEASE-0.5.0-CHECKLIST.md; `--force` remains the workaround until then.**
- [x] P0 | 2026-08-18 | app/src-tauri/tauri.conf.json:5 | THE RENAME RELEASE MUST BE CUT AS 0.5.0, NOT 0.4.6. `identifier` moved `com.winmux.app` → `com.ymux.app`, which is the MSI/NSIS upgrade key: Windows treats the new package as a DIFFERENT product, so it installs SIDE BY SIDE and the old winmux stays in Programs and Features with its own Start Menu entry. There is no shim for this — the identifier IS the identity. Data is safe (ymux-core `config_dir` renames `%APPDATA%\winmux` → `%APPDATA%\ymux` on first boot), but the release notes must tell users to uninstall the old winmux AFTER the first successful ymux launch, in that order — uninstalling first is harmless, uninstalling while both are installed is what the migration expects, and running BOTH at once hits the P1 shared-config-dir corruption above with the extra twist that they now use different directories. **CLOSED 2026-08-23 — nothing left to do here; this is a duplicate of the release checklist, not an open defect. Confirmed in tree: app/src-tauri/tauri.conf.json has `version: 0.5.0`, `identifier: com.ymux.app`, `productName: ymux`. The requirement itself is blockers 0.1 and 0.2 in docs/RELEASE-0.5.0-CHECKLIST.md, which carry both halves — the never-cut-as-0.4.6 rule AND the release-note ordering (install YMUX → launch once so the `%APPDATA%` folder migrates from winmux to ymux → only then uninstall old winmux). The residual risk is human, not technical: someone cutting 0.4.6 by habit. That risk lives in the checklist, which is read at cut time; this file is not.**
- [x] P0 | 2026-08-18 | server-side, no known code path | tmux sessions vanish from the SERVER after an abrupt client loss (laptop unplugged from AC). Yossi confirms `tmux ls` was already empty BEFORE his manual reboot, so the reboot is not the cause. NO MECHANISM FOUND: every kill path in the repo was read and excluded — the 4 `tmux kill-session` sites (all behind explicit user action: PaneView.tsx:951 -> App.tsx:1655 -> lib.rs:6524, and rpc_server.rs:1745 via `ymux pane-disconnect --kill`), `kill_session_inner` (lib.rs:3288, explicitly preserves tmux), the bootstrap `pkill` (tmux's cmdline is `tmux -f $HOME/.ymux/tmux.conf new-session…`, which never matched the pattern), the port-watch reaper (scoped to `port-watch [-]-workspace X`), the addons pkill (scoped to ymux-server/insights), and the Go hygiene daemon (server/internal/insights/hygiene.go: `killPids` permits ONLY duplicate port-watchers by PID; orphan claude needs etime > 24h and is never auto-killed). No `destroy-unattached`/`exit-empty` in the shipped conf. Two candidates left, both needing evidence rather than code reading: (a) something outside ymux — OOM killer on a shared box that was sitting at load average 22 with 10 users; (b) the `tmux ls` looked at a different socket (other user, other `TMUX_TMPDIR`) while the sessions were alive. NEXT STEP is data, not a fix: `journalctl -k --since '-7d' | grep -iE 'oom|killed process'`, `journalctl --list-boots`, and `id -un; echo $TMUX_TMPDIR; ls -la /tmp/tmux-$(id -u)/`. The new `tmux-inventory:` log line (lib.rs, before each attach) timestamps the disappearance from our side. Do NOT ship a fix on a guess — three hypotheses have already been falsified (systemd-logind KillUserProcesses, an external reboot, and any in-repo kill path). **MOVED 2026-08-23, NOT SOLVED — read this before assuming it is done. No mechanism has been found and no fix has been shipped. It leaves FOLLOWUPS because this file is a list of defects with known causes, and this is an open investigation: it lives in full under docs/DECISIONS.md § Open, '2026-08-18 — Why do tmux sessions vanish server-side after an abrupt disconnect? NO MECHANISM FOUND', which is scanned at session start per CLAUDE.md. State unchanged: three confident hypotheses falsified (systemd-logind KillUserProcesses, an external reboot, every kill path in the repo), two candidates left (the OOM killer on a shared box at load 22, or `tmux ls` reading a different socket). The `tmux-inventory:` line in app/src-tauri/src/lib.rs timestamps it from our side. The next step is still DATA, on the server, not code: `journalctl -k --since '-7d' | grep -iE 'oom|killed process'` and `id -un; echo $TMUX_TMPDIR; ls -la /tmp/tmux-$(id -u)/`. Do not ship a fix on a guess.**

<!-- Found while fixing the macOS ports + upload bugs (2026-08-10). All confirmed
     by reading the code, none of them reproduced live. Out of scope for that fix. -->
- [x] P1 | 2026-08-10 | app/src-tauri/src/provisioning.rs:278 | `dpapi_protect` on non-Windows returned `noprotect:<secret>` — remembered SSH passwords sat in plaintext at rest on macOS, violating Rule #2. FIXED 2026-08-20 (Phase 82.D): the non-Windows arm returns `Err`, which Rule #2 names explicitly ("otherwise keep in memory only"), and `save_workspace_secret` wraps BEFORE opening the file so nothing half-writes. Deliberately NOT Keychain — see the new P2 below: nothing reads this store back.
- [x] P1 | 2026-08-10 | app/src-tauri/mcp/src/main.rs:179 | MCP server hard-errored on macOS ("only Windows transport is implemented"). FIXED 2026-08-20 (Phase 82.D): walks the same Unix-socket candidate list the server binds. Needs a live run on a Mac — no CI test exercises it.
- [x] P1 | 2026-08-10 | app/src-tauri/cli/src/main.rs:1171 | Local CLI had no Unix-socket transport, so every local hook / `ymux` call on macOS exited 2 unless the socket addr was set by hand. FIXED 2026-08-20 (Phase 82.D): dials `$TMPDIR/ymux-<user>.sock`, the `<config>/rpc.sock` sun_path fallback, then the pre-rename name. The other half — "CI builds no mac-native CLI to ship it in" — is fixed by 82.E, which stages `resources/ymux-cli` and lists it in tauri.macos.conf.json. Needs a live run.
- [x] P2 | 2026-08-10 | app/src-tauri/src/updater.rs | In-app updater is NSIS/MSI-only. PARTIALLY FIXED 2026-08-20 (Phase 82.D): it was not a no-op, it was harmful — off Windows it downloaded the `-setup.exe`, spawned it, and exited the app 800 ms later regardless of the spawn failing, so "Install update" quit ymux and installed nothing. Both install commands now refuse up front. A real mac updater (mount the .dmg, swap a running .app) is tracked as the new P3 below.
- [x] P3 | 2026-08-10 | app/src-tauri/src/settings.rs:1661 | No font enumeration on macOS/Linux, so the picker only ever offered the hardcoded baseline. FIXED 2026-08-20 (Phase 82.D): scans ~/Library/Fonts + /Library/Fonts + /System/Library/Fonts (macOS) or the XDG/fontconfig dirs, reading each face's name table via `fonts::full_font_name`. Non-recursive and capped at 2000 faces; an empty scan still returns None so the "assume installed" fallback holds. Also fixed the install side, which wrote into `~/Library/Application Support/Microsoft/Windows/Fonts` and then failed at registration.
- [x] P3 | 2026-08-10 | app/src-tauri/src/insights_local.rs:283 | Docker hint text hardcodes `\\.\pipe\docker_engine` even on the non-Windows failure path. FIXED 2026-08-17 — unix connect_docker probes DOCKER_HOST, /var/run, ~/.docker/run, OrbStack, Colima, Rancher, Podman sockets and reports the real path.
- [x] P3 | 2026-08-10 | app/src-tauri/src/local_setup.rs | The "local → new" smart-setup wizard is WSL/winget-only (~1200 lines) with no macOS equivalent; every step fails with ENOENT there. Not cfg-gated, so it's reachable from the UI. FIXED 2026-08-17 on macos-build — per-OS step engine: brew/git/node/claude/codex/gemini + local tmux persistence chain (InstallTmuxLocal/DeployTmuxConfLocal), finalize_local_workspace. Hooks still unavailable on mac (CLI not bundled — see P1 above).
- [x] P2 | 2026-08-10 | app/src-tauri/src/lib.rs:1518 | `pick_default_shell()` is not cfg-gated and falls through to `"cmd.exe"` on every OS — creating a local workspace on macOS spawns a binary that doesn't exist. FIXED 2026-08-17 on macos-build — cfg-gated: $SHELL/zsh/bash/sh default, zsh/bash/fish/pwsh/sh detection, native-separator cwd defaults.
- [x] P2 | 2026-08-10 | app/src-tauri/src/local_wizard.rs:120 | `detect_local_shells()` pushes "Windows PowerShell" and "Command Prompt" with `available: true` regardless of host OS. FIXED 2026-08-17 on macos-build — cfg-gated: $SHELL/zsh/bash/sh default, zsh/bash/fish/pwsh/sh detection, native-separator cwd defaults.
- [x] P2 | 2026-08-10 | app/src-tauri/src/local_wizard.rs:251 | `builtin_defaults()` backslash-joins cwd suggestions (`{h}\Documents`) — shows `/Users/yossi\Documents` on macOS. FIXED 2026-08-17 on macos-build — cfg-gated: $SHELL/zsh/bash/sh default, zsh/bash/fish/pwsh/sh detection, native-separator cwd defaults.

- [x] P0 | 2026-08-18 | manifest.json, hooks/*.json, app/src-tauri/cli/src/hooks.rs:152, README.md, docs/RELEASING.md | GitHub repo rename — ALREADY DONE, found by accident. Filed as a blocker on the assumption the repo was still `winmux`; the push to main answered with `remote: This repository moved ... yyhezkel/ymux.git`, i.e. Yossi had already renamed it. Verified rather than assumed: `gh api repos/yyhezkel/ymux` -> full_name=yyhezkel/ymux, default_branch=main; `raw.githubusercontent.com/yyhezkel/ymux/main/hooks/claude-code.json` -> 200, so `load_spec`'s canonical URL resolves; the OLD raw URL also -> 200 via GitHub's redirect, so pre-rename CLIs still fetch the spec. Local `origin` was still on the old URL (working only through the redirect) and was repointed with `git remote set-url`. Note this is the SHARED .git/config — the main checkout and every other worktree pick it up too. `manifest.json` stays un-rewritten on purpose: it describes the published v0.4.5-beta.1 assets, which really are named `winmux_0.4.5_*` and are still served from the old release URLs via the same redirect.

- [x] P2 | 2026-08-11 | app/src/browserDevMode.ts:72 | `saveProjectOverride` had no caller, so rung 1 of the tickets project ladder was unreachable from the UI. FIXED 2026-08-12 by e0c32d6 — a text field + Apply/Use-default in the capture modal's destination row, which refetches the resolution so the new target is visible before saving. Deliberately not a folder browser: Phase 54 was reverted for making the user browse an SFTP tree for something the app derives.
- [x] P3 | 2026-08-11 | app/src/TicketModal.tsx:71 | `screenshot_data_url` was hardcoded null, so no ticket could ever have an image despite a complete Rust decode/write path. FIXED 2026-08-12 by 76ec575 — the element is rasterized in-page via an SVG foreignObject (no library injected), with computed styles inlined because a node cloned into foreignObject inherits nothing from the document's stylesheets. Degrades to null on tainted canvas / oversize / timeout rather than to a wrong ticket. Read back through tickets_screenshot as a data-url so remote tickets work identically.

- [x] P1 | 2026-08-09 | app/src-tauri/src/local_setup.rs:1195 | Elevated `wsl --install` is undiagnosable: `Ok(Ok(code)) => Ok((code, String::new()))` hands `classify_wsl_install` an EMPTY output string, because ShellExecuteEx("runas") gives no stdout/stderr pipe. So on the elevated path the text fallbacks (`contains("elevat")`, `contains("reboot")`) are dead code, and every code that is not 0/3010/1223/740 collapses to `StepFailed { exit_code, stderr: "" }`. HIT LIVE 2026-08-09 on Yossi's box: `wsl --install --no-launch -d Ubuntu` exited 1 after 23s of real work and the UI could only say "exit 1" — no reason, nothing in debug.log. The doc comment calls the lost pipes "an acceptable trade because the codes it does give us are unambiguous", but generic 1 is the common case and is exactly the ambiguous one. Fix: relaunch elevated via `cmd /c "wsl --install ... > %TEMP%\ymux-wsl.log 2>&1"` (or a ShellExecuteEx'd helper that redirects) and read the file back after the wait, so the real message survives. FIXED 2026-08-09 by fd1f537 — elevate `cmd /s /c` and redirect into a per-run temp file (path allowlisted by the new cmd_safe_path, WSL_UTF8=1 so the text is UTF-8), read it back after the wait, delete on every path. Exit code now logged on success too. Quoting verified against a real cmd.exe with --status swapped in for --install: redirect lands, 0 NUL bytes, real output captured. 5 new unit tests.
- [x] P1 | 2026-08-03 | app/src-tauri/src/lib.rs:1565 | emit_data strict-UTF8 decoder: if `leftover` starts with an invalid byte, `valid_up_to==0` returns WITHOUT draining -> that pane goes permanently silent and the buffer grows unbounded (no cap, no error branch). Found while tracing the local-PowerShell Hebrew bug. FIXED 2026-08-03 — decoder moved to `pty_decode::Utf8Stream`, which branches on `Utf8Error::error_len()`: a truncated tail is held (Hebrew/emoji splits still survive), genuinely invalid bytes become U+FFFD and are drained, so a stall is structurally impossible and leftover is capped at 3 bytes by construction. 14 unit tests.
- [x] P2 | 2026-08-03 | app/src-tauri/src/local_setup.rs:1096 | local-setup failures log only `err.user_message()`, dropping `stderr` -> the UI red card shows the real reason but the log never does, so a user-reported install failure is undiagnosable after the fact. FIXED 2026-08-03 by 6753759 — failure logs now append StepFailed stderr.
- [x] P1 | 2026-08-19 | app/src/sidebar.css, app/src/Sidebar.tsx | VERIFIED LIVE by Yossi on the branch build (a0ce0d0, run 32235591934), across three runs. Confirmed working: the colour flood gone and rows uniform in Hebrew/RTL light mode; the badge column straight; row names all starting on one vertical line; the masthead scaling with the rail; double-click on the wordmark collapsing and expanding; Tab-to-wordmark + Enter doing the same. Two real defects were found by running it that the fixture had missed and are now fixed in f6f588f and a0ce0d0 -- see the entry below for the one that is still open.
- [x] P1 | 2026-08-20 | app/src-tauri/src/lib.rs (sanitize_tmux_session_name_for_title ~1616, effective_tmux_name ~6723, build_zellij_attach_command ~2374) | **A PANE TITLE CAN BREAK ITS OWN SESSION NAME, and it may be part of "kill did nothing".** `build_zellij_attach_command` claims in a comment that "session names come from `sanitize_session_name`, which emits only `[A-Za-z0-9_-]`, so there is nothing here to quote or escape". No function has that name, and the claim is FALSE for the title path: `effective_tmux_name` prefers `sanitize_tmux_session_name_for_title`, which substitutes only `.`, `:` and whitespace. So `;`, `&`, `|`, `$`, backticks and quotes survive into `format!("zellij attach -c {name} **FIXED 2026-08-23. The comment was the bug's best evidence: `sanitize_session_name` has never existed in this tree, and the function that actually feeds the attach line — `sanitize_tmux_session_name_for_title` — substituted only `.`, `:` and whitespace, so `;`, `&`, `|`, `$`, backticks, quotes, `>` and `^` all rode a pane title into a line typed verbatim into cmd.exe or PowerShell.** Fixed at the SOURCE, not by quoting at the use site, because no quoting is correct in cmd.exe and PowerShell at once — they disagree about `^`, `%`, backticks and single quotes, and the attach line has to parse in both. New `session_name_char_is_safe` is a whitelist: ASCII alphanumerics, `_`, `-`, and any non-ASCII character that is neither a control nor a separator. That last clause is what preserves Phase 23.I's promise (a Hebrew/CJK title becomes a session of the same name) with no special case, since every metacharacter in all three shells is ASCII — niqqud and other combining marks pass through whole. `build_zellij_attach_command` now returns `Option` and REFUSES a name it cannot type; mangling would silently attach to a session other than the one picked, whereas refusing leaves the pane in a plain working shell, the same failure mode it already accepts when zellij is missing. The refusal path can only trigger on a picker-supplied name, i.e. a session created outside ymux by hand. Audited the neighbours while here: the tmux/SSH attach script and `tmux kill-session` were already correct (`shell_quote`), and `zellij_args_delete_force` was already argv-only — the Windows zellij line was the single hole. Six tests added, five of them hostile-input; the test that was supposedly guarding this line only ever fed it `ymux-p_1a2b_0`, a name that could not fail, so it passed for as long as the hole existed. **CI-verified only (Rule #17); the live check is the one this entry always wanted — title a pane `work; calc` on Windows, confirm no `calc`, and confirm Kill session still finds it.**

")`, which is typed VERBATIM into cmd.exe or PowerShell — where `;` is a statement separator. Self-inflicted (the user's own title, their own shell), so not a security emergency, but the mundane consequence bites regardless: zellij creates the session under a different name than ymux stored, so `list-sessions`, kill and restore all silently fail to match it. The test that should have caught it, `zellij_attach_command_is_a_single_safe_line`, only ever exercises the already-safe `ymux-p_1a2b_0`. Fix sketch: a `sanitize_zellij_session_name` reducing to `[A-Za-z0-9_-]`, applied ONLY when deriving a NEW name, never when attaching to one the user picked from the list. NOT done with the 2026-08-20 lock work because changing name derivation strands every existing session whose name holds punctuation or Unicode — it needs its own migration and its own live test.
- [x] P1 | 2026-08-18 | app/src-tauri/crates/winmux-core/src/lib.rs:41 (config_dir) | TWO WINMUX BUILDS SHARE %APPDATA%\winmux AND THE OLDER ONE SILENTLY STRIPS NEWER FIELDS. Hit live: winmux-beta3-MEGA-test.exe was running beside a dev build of the project-folders branch; it predates parent_id / is_project_root, serde ignores the unknown keys on load, and save_to_disk rewrites the WHOLE file from the struct — so every save by the old app erased the workspace nesting. Symptom is maximally misleading: pinned folders appear as separate top-level workspaces, worktrees stop listing, and NOTHING appears in the log of the new build, because it did nothing. Also fought over debug.log (alternating 4040/4060-byte saves are the two writers). Workaround that exists today: WINMUX_CONFIG_DIR isolates a build (`$env:WINMUX_CONFIG_DIR="$env:APPDATA\winmux-dev"`). Fix options: (a) refuse to start when another winmux holds the same config dir (lock file with the pid), or (b) stamp a schema version in workspaces.json and refuse to WRITE when the file's version is newer than the binary's — losing user data to an older build is the dangerous half. (b) is the real fix; (a) is the cheap one. **FIXED 2026-08-23, with option (b) as this entry specified — and read the limit, it is real.** `workspaces.json` carried a `version` field since forever that nothing ever read; it is load-bearing from now on. `WORKSPACES_SCHEMA_VERSION` is 2, stamped on every write via `serialize_with` rather than by assigning the field (the invariant is "what we WRITE is current", and serialization is the one place that cannot be bypassed; an assignment would have to be repeated at every construction site and would drift). `schema_gate(on_disk, last_written)` was extracted as a pure function — same reason `resolve_effective_session_name` was — because the interesting cases need two binaries sharing a config dir, which no test can arrange. Three outcomes: **Refuse** (on-disk newer than this build — the dangerous direction, a hard `Err` from `save_to_disk`, because merging would need us to understand keys we have never heard of), **WarnDowngrade** (on-disk older than what we last wrote: an old build got here first — write anyway, since refusing would leave the user unable to save for as long as that build is open, and the three-way merge is the actual repair), **Write** otherwise. `schema_version_of` is deliberately tolerant: absent, empty, malformed, version-less and non-integer all read as `None` = carry on, because a refusal hangs off this and a stray byte must never lock a user out of saving. **THE LIMIT, stated plainly: this does nothing about the 0.4.x builds already on disk.** They never read the field and will write v1 back over a v2 file, so the incident as originally reported is NOT prevented going backwards — what covers that is `workspaces_merge`, which already re-reads and merges and treats a vanished field as an absence rather than a deletion (see the P2 "TWO YMUX BUILDS SHARING %APPDATA%\ymux — was P1, now largely handled"). What this adds for the old-build case is the LOG LINE, which is the half that was missing: the entry's own words were "NOTHING appears in the log of the new build, because it did nothing". Option (a), the pid lock file, is NOT done and remains the only thing that would stop an already-shipped build — filed to BACKLOG rather than left implied here. Seven tests. CI-verified only (Rule #17).
- [x] P1 | 2026-08-03 | app/src-tauri/src/fonts.rs | Install has no matching UNINSTALL. A user who installs FiraCode Nerd Font (27 MB, 6 files) has no in-app way to remove it — they must hand-delete from %LOCALAPPDATA%\Microsoft\Windows\Fonts and HKCU. Symmetry matters here because we are the ones who put the files there. **FIXED 2026-08-23.** `font_uninstall(id)` mirrors `font_install(id)`: delete the faces from the per-user font directory, then drop their HKCU registrations. **Which files belong to an entry is derived from the CATALOG, not from a record of what was installed** — a zip asset wrote everything matching its `ZipFilter::name_prefix`, a bare asset wrote exactly its `save_as`, so the same data identifies them again. That choice is the whole point: a manifest written at install time would only know about installs done after it shipped, leaving every font already on a user's machine — precisely the ones this entry was filed about — permanently unremovable. Order is delete-then-unregister, because the reverse leaves a font still on disk and still loadable with no registry entry: present but invisible to the picker, worse than either end of the operation. Registry values are matched by their DATA (the path) rather than by rebuilding the value name, since `register_font` derives that name from the font's internal name table with a fallback to the file stem, and rebuilding it would mean reproducing that decision from a file that has just been deleted. A face held open by a running app is a REPORTED outcome (`failed`), not an error — the rest of the family still comes out, and the UI says which ones did not. `font_catalog` grew an `installed` flag, read from the directory on every call. **UI: a new "Installed by ymux" list under both font pickers, with a Remove button per row, rendering nothing when nothing is installed.** It is a list rather than a button beside the picker because `FontMissingNotice` only renders while a family is MISSING, so an installed font had nowhere to hang a control. `window.confirm` guards it (deleting files; the way back is a multi-MB download) — consistent with `FileEditor`, though note the codebase has been moving destructive *workspace* actions to a styled component, and this could follow later. i18n in all four locales. Four tests, including one that pins that no catalog `name_prefix` is a prefix of another (`FiraCode-` vs `FiraCodeNerdFontMono-` share a stem, and a future entry could silently make one swallow the other) and one that an unknown id is refused before anything is deleted. **CI-verified only (Rule #17); the live check is install then remove on Windows, confirming the picker flips back to warning and the files and HKCU values are actually gone.** Known and accepted: a user's own hand-installed copy of the same family, in the same per-user directory, would also be removed. It is the family they asked to remove, and the alternative is the manifest that cannot see the past.
- [x] P0 | 2026-08-23 | .github/workflows/ci-windows.yml (Vault freshness gate) + docs/vault/*.md | **`main` IS RED, AND NOTHING REAL IS BEING CHECKED ON IT.** The Phase 85 vault gate runs BEFORE `cargo test`, `tsc`, the frontend build and the whole Go half — so when it fails, none of them run at all. It has failed on every push to `main` since PR #18: runs 32654312291 (#20) and 32654357741 (#22) both died in under a minute on `Vault freshness gate`, and every step after it shows as skipped. Not a flake and not branch-specific — a `workflow_dispatch` on `yos/folow-ufp-review-fbbfda` (run 32656549376) fails identically. **Cause is the ordinary one: the gate landed as Phase 85 while four other PRs were in flight** (#20 #21 #22 #23), they merged code without updating the prose that explains it, and the lock went stale. NINE vaults are flagged: backend-claude, backend-core, backend-panes, build-glue, frontend-flows, frontend-lib, frontend-panes, frontend-shell, server-go (coverage reports 92,602 / 99,561 lines, 93%, 16 vault files). **Do NOT just run `node scripts/vault-check.mjs --write`** — the script's own header says a bare re-stamp makes the gate theatre, and its error text is explicit: fix the prose, THEN re-stamp, and commit both. `[vault-skip]` in a PR title does not rescue it either: that only skips the DIFF check and the HASH check is the half that is failing. Whoever fixes it needs to know what the four merged PRs actually changed, which is why this is filed rather than guessed at. Until it is fixed, every branch is unverifiable and "CI green" is unavailable as a check — including for the three fixes committed on 2026-08-23 (Phase 84.F/G/H), which are therefore NOT compiled, not just not run. **FIXED 2026-08-23, and the fix was the prose, not the stamp.** Nine vaults were stale from two sources: the four PRs that merged while the Phase 85 gate was in flight, and this session's own Phase 84 work. Each covered file was diffed against the last stamp (`57fc95d`) and the explanation rewritten from the diff — `/analytics` and `/claude-usage` in server-go, `claude_usage_local.rs` in backend-claude, the schema gate and session-name whitelist and the two `.devtools(false)` calls in backend-core, the macOS JS probe and font uninstall in backend-panes, the `devtools` Cargo feature in build-glue, and the new Insights panels and pure modules across the frontend three — and only then `node scripts/vault-check.mjs --write`. That order matters and is not ceremony: the script's own header says a bare re-stamp makes the gate theatre. Verified locally on both halves, including `--diff-base origin/main` for the PR path, then confirmed green in CI run 32658010137 where the gate passed and `cargo test` ran for the first time since PR #18. **A SECOND SESSION FILED THIS INDEPENDENTLY** (`a0a3598` on main, "docs(followups): main's CI has been red since 17:16") — the two entries were merged into this one rather than carried separately, since it is one incident. **THE GATE HAS A BLIND SPOT, now its own P2:** eleven new source files were in NO vault's `covers:` list, so the hash check never mentioned them — it only compares files already claimed. `claude_usage_local.rs` (549 lines), both new Insights panels, `insightsReport.ts`, `claudePricing.ts` and more were invisible to a gate that was otherwise passing. All eleven are now covered; coverage went 93% → 96% (217 → 225 files).
- [x] P0 | 2026-08-23 | docs/vault/.vault-lock.json + 7 vault pages | **MAIN HAS BEEN RED SINCE 17:16, AND IT IS THE VAULT GATE, NOT ANY CODE.** Last green ci-windows on main is PR #18 (16:46, run 32652717148); #20, #22 and the 84.F CSS push all fail at the "Vault freshness gate" step. The cause is a merge-order accident, not a bad change: PR #20 IS the vault (Phase 85 — it introduced `.vault-lock.json`) and it merged at 17:16:35, inside a 90-second window with #23 (17:16:18), #21 (17:13:55) and #22 (17:17:28), all of which branched before the lock existed and landed covered code the lock had never hashed. Stale now: backend-claude (insights_local.rs), backend-core (lib.rs), backend-panes (workspace_browser.rs), build-glue (Cargo.toml), frontend-panes (BrowserWindow.tsx, InsightsWindow.tsx), frontend-shell (App.tsx), server-go (service.go plus FOUR never-summarised files: analytics.go, analytics_test.go, claudeusage.go, claudeusage_test.go). **Two consequences, both worse than "CI is red" sounds.** (1) The gate runs FIRST and the job dies in ~30s, so cargo test / tsc / vite never execute — every push to main since 17:16 is completely ungated, 84.F included. Red here means "CI did not run", not "CI found something". (2) The `[vault-skip]` escape hatch cannot save a direct push: it reads the PR TITLE, and a push to main has none. To close: write the prose for all seven pages (the four new Go files need real summaries, not a re-stamp), then run vault-check with --write and commit prose and lock together — re-stamping alone does not pass, the gate also requires the owning .md to appear in the diff. **FIXED 2026-08-23 — and this is the THIRD independent filing of one incident.** Kept rather than deleted as a duplicate, because its forensics are the best account of the CAUSE that anybody wrote: PR #20 *is* the vault (Phase 85, the commit that introduced `.vault-lock.json`) and it merged at 17:16:35 inside a 90-second window with #23 (17:16:18), #21 (17:13:55) and #22 (17:17:28) — all three branched before the lock existed and landed covered code it had never hashed. A merge-order accident, not a bad change, and not something review of any single PR would have caught. The repair is recorded on the entry archived just above this one ("`main` IS RED, AND NOTHING REAL IS BEING CHECKED ON IT"): nine vaults re-written from the diffs against the last stamp, then re-stamped in that order, verified green in CI run 32658655727. **The recurring lesson costs more than the bug did:** three sessions spent effort discovering the same red build, and two of them wrote prose for the same nine pages, which then had to be reconciled by hand across two more merges. A gate that lands while other PRs are in flight will do this every time — the cheap prevention is to merge a repo-wide gate on a quiet tree, or to land it warn-only for one cycle and flip it to blocking afterwards.
- [x] P1 | 2026-08-23 | app/src-tauri/Entitlements.plist + app/src-tauri/tauri.macos.conf.json:15 | **HARDENED RUNTIME HAS NEVER BEEN LAUNCHED.** `claude/macos-universal-signing` turns on `"hardenedRuntime": true` and adds a brand-new Entitlements.plist; before it, mac bundles ran with neither. Under hardened runtime a MISSING entitlement does not fail the build — it fails at launch, typically as a blank window with nothing useful in the Rust log, so CI cannot catch it and run 32639916194 being green proves only that it compiles and bundles. Deferred by Yossi 2026-08-23: no Mac at hand, a contributor will test later. **To close it:** `gh run download 32639916194 -n ymux-macos-arm64-dmg` (or `-n ymux-macos-x64-dmg` on Intel; artifacts expire after 90 days, re-dispatch the workflow if so), open the dmg, drag to Applications, launch, and check (a) the window actually renders rather than staying blank — that is the JIT entitlement, (b) a local pane spawns zsh — that is `disable-library-validation`, since the bundled ymux-cli carries a different signature than the host, (c) SSH out to a remote works — network.client, (d) the local-workspace folder picker opens — files.user-selected.read-write. Each entitlement in the plist maps to one of those, so a specific failure names the specific missing key. If it is blank/crashing, the fix is an added `com.apple.security.cs.*` key, NOT reverting hardenedRuntime — notarisation requires it, so the signed path needs it anyway. **UPDATE 2026-08-23:** the "CI cannot catch it" half is now false for the *plumbing* — `build-macos-intel.yml` asserts the `runtime` flag, all six entitlement keys, the absence of `app-sandbox`, and that `Info.plist` merged; those checks used to be prints ending in `|| true`. A missing key now fails the build. The *launch* half stands: only a real Mac proves the app renders and spawns a shell. **CLOSED 2026-09-01:** overtaken by reality — Yossi reports the office Mac runs the app live, tests included, so the hardened-runtime build launches, renders and spawns shells (the per-entitlement walk above was never run as a formal checklist; it stays useful as the diagnostic if a specific entitlement ever regresses). The v0.5.0 release ships both dmgs on the strength of that.
