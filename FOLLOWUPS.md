# Followups

Out-of-scope bugs found in passing. One line per item. Read at session start;
surface P0/P1 to the user before starting new work.

Closed items are moved to `FOLLOWUPS-ARCHIVE.md` with their full text and their
closing note — check there before re-investigating something that looks familiar.
Everything below this line is OPEN.

Format:

```
- [ ] P<0|1|2|3> | <YYYY-MM-DD> | <file>:<line> | <one-line repro/symptom>
```

## Open

- [ ] P1 | 2026-09-02 | app/src-tauri/src/sessions_overview.rs + app/src/SessionsOverviewWindow.tsx + lib.rs (tmux_rename_session) | NOT VERIFIED LIVE: all of Phase 90 (the active-sessions overview). CI green is Rule #14 "compiles". Smoke, in order: (1) SSH workspace with 3+ sessions across 2 directories → right-click → "סשנים פעילים" → the table is grouped by directory and the summaries + status pills land within ~30s and match what is actually on each screen (a Claude waiting on a permission card must read `waiting_input`); (2) פתח (90.B) → the CURRENT screen is untouched; a new row appears under the server — or under the pinned folder whose directory contains the session's — attached to the session, and `grep attach-only %APPDATA%\ymux\debug.log` shows the guard fired — NOT ONE BYTE typed; press פתח AGAIN on the same session → the existing row is activated, no duplicate; delete the row → `tmux ls` still lists the session; restart the app → the row is still there, and a plain [Connect] on its pane ATTACHES to the session (does not spawn `ymux-p_…`); (3) סגור סשן → row gone, `tmux ls` on the host agrees; kill a session one of OUR panes holds and confirm the pane goes too; (4) שנה שם → `tmux ls` shows the new name, the attached pane's Kill button still works (live Session field migrated), the picker still shows it as owned (session-owners migrated), a Hebrew name is refused with the ASCII hint; (5) Windows local: zellij rows appear grouped under their owner cwd, Rename is disabled with the tooltip, `dump-screen` summaries land, an EXITED row shows no spinner; (6) macOS local: same via the local tmux + local claude; (7) `debug.log` contains NO screen text and NO summary text — only `summarize: ws=… sessions=N parsed=…` lines (Rule #1); (8) a workspace with every pane closed → פתח still works (workspace_reset_layout path).
- [ ] P3 | 2026-09-01 | app/src-tauri/src/claude_summary.rs:18-22 | The module doc-comment claims "Local-only workspaces fall through to a Windows-side claude.exe lookup if present" — **no such fallback exists**: `pick_handle` matches `Session::Ssh` only, so `auto_summarize_on_stop` errors with "no active SSH session" on every Local/WSL workspace. Fix the comment, or implement the local path if the summary feature is ever pointed at local panes. Found while designing BRIEF (which deliberately does NOT reuse this machinery).
- [ ] P3 | 2026-08-24 | .github/workflows/build-macos-intel.yml § Create dmg | `hdiutil: create failed - Resource busy` on the arm64 job, run 32667104019. Everything that compiles the tree passed (cargo test, tsc, frontend build, the .app bundle, the arch + signature assertions) and only the dmg packaging step blew up; `gh run rerun --failed` went green on the same SHA. Known GitHub macOS-runner flake, not ours. If it starts costing reruns, the fix is a retry loop around `hdiutil create` (it also wants `-nospotlight`), not a code change — but leave it until it actually recurs.
- [ ] P3 | 2026-08-23 | docs/vault/*.md (cross-vault references) + scripts/vault-check.mjs | **32 CROSS-VAULT REFERENCES IN THE PROSE, AND NOTHING CHECKS ANY OF THEM.** Vault pages point at each other in plain text ("see `backend-panes.md`", "see `crates.md`") — 32 of them today, and all 32 currently resolve, which is luck rather than enforcement: renaming or merging a vault page breaks every reference to it silently, and the freshness gate has no opinion because it only hashes SOURCE files. Raised by Yossi asking whether the vault should be Obsidian-shaped (`[[wikilinks]]` + `moc.md`), which would make the checker he found the right tool. Measured before answering: every vault is already reachable from `INDEX.md`, so the moc half is satisfied under a different name; the 16 pages run 130–220 lines each, which is the right chunk for the actual consumer — an agent reading whole files into a context window, not a human clicking a graph. Atomic notes plus dense linking buys navigation nothing here uses and costs round trips. **So take the checkability, not the format:** teach `vault-check.mjs` to resolve `` `name.md` `` references against the vault directory and fail on a miss. Same class as the `file:line` cite-rot P3 above — a pointer in prose that nothing verifies — and one pass should close both.

- [ ] P3 | 2026-08-23 | docs/vault/*.md (`file:line` citations) + scripts/vault-check.mjs | **A `file:line` CITE IN A VAULT ROTS SILENTLY AND NOTHING CHECKS IT.** Four of the six in the tree were wrong when measured on 2026-08-23 and were fixed by hand: `AppState` cited at lib.rs:145 (really 146), `save_to_disk` at 514 (838), `pane_connect` at 7419 (7949), `emit_data` at 1960 (2370). The two `rpc_server.rs` cites were still right. The hash gate does not help: it fires when the file changes, which is when someone re-reads the PROSE, and a line number in the prose is exactly the thing they do not re-derive. **A range check is NOT the fix** — this was measured against a candidate script (`vault-check.sh`, Obsidian-style, checks that a cited line is within the file): lib.rs is 13,272 lines, so all four wrong cites are comfortably "in range" and it would have passed every one of them. The fix that works is an IDENTITY check, not a bounds check: write the cite as file:line plus the symbol it points at, and verify the symbol is on or near that line — `save_to_disk@lib.rs:838` fails loudly the moment the function moves, and can be auto-corrected by search rather than by a human recounting. Low priority because six cites is a small surface; it grows with the vault, and the same check would let prose cite lines freely instead of avoiding them.

- [ ] P2 | 2026-08-23 | scripts/vault-check.mjs (the hash check) | **THE VAULT GATE IS BLIND TO CODE NOBODY CLAIMED.** The hash check compares each vault's `covers:` list against `.vault-lock.json`, so a source file that is in NO vault's list is not drift — it is invisible. The tree can therefore grow code the vault does not explain while the gate stays green, which is the exact failure the vault exists to prevent (`docs/MODULES.md` claiming lib.rs was ~1,760 lines while the real file was 12,475). Found live on 2026-08-23: ELEVEN files were uncovered and unmentioned — `claude_usage_local.rs` (549 lines), `InsightsAnalytics.tsx` (622), `InsightsClaudeCost.tsx` (458), `insightsReport.ts` (427), `claudePricing.ts` (246), `insightsCommands.ts`, `insightsFmt.ts`, `clipboardText.ts` and their tests. They were added by hand and coverage went 93% → 96%; nothing would have caught the next batch. The only existing signal is the coverage NOTICE, which by design never fails — and a percentage that drifts down by one point per merge is not something anyone notices. Note the asymmetry that hid it: `server-go.md` uses globs (`internal/insights/*.go`) so its new files DID surface as `+ newly covered`, while every Rust and frontend vault lists files explicitly and cannot. Options: (a) fail when a file matching `CODE_EXT` is owned by no vault, with an explicit `unowned:` allowlist for things that genuinely need none; (b) fail when coverage drops below a floor committed in the lock; (c) move the explicit lists to globs, which is the smallest change but gives up the deliberate control over what a vault claims. (a) is the real fix.

- [x] P0 | 2026-08-23 | docs/vault/.vault-lock.json + 7 vault pages | **MAIN HAS BEEN RED SINCE 17:16, AND IT IS THE VAULT GATE, NOT ANY CODE.** Last green ci-windows on main is PR #18 (16:46, run 32652717148); #20, #22 and the 84.F CSS push all fail at the "Vault freshness gate" step. The cause is a merge-order accident, not a bad change: PR #20 IS the vault (Phase 85 — it introduced `.vault-lock.json`) and it merged at 17:16:35, inside a 90-second window with #23 (17:16:18), #21 (17:13:55) and #22 (17:17:28), all of which branched before the lock existed and landed covered code the lock had never hashed. Stale now: backend-claude (insights_local.rs), backend-core (lib.rs), backend-panes (workspace_browser.rs), build-glue (Cargo.toml), frontend-panes (BrowserWindow.tsx, InsightsWindow.tsx), frontend-shell (App.tsx), server-go (service.go plus FOUR never-summarised files: analytics.go, analytics_test.go, claudeusage.go, claudeusage_test.go). **Two consequences, both worse than "CI is red" sounds.** (1) The gate runs FIRST and the job dies in ~30s, so cargo test / tsc / vite never execute — every push to main since 17:16 is completely ungated, 84.F included. Red here means "CI did not run", not "CI found something". (2) The `[vault-skip]` escape hatch cannot save a direct push: it reads the PR TITLE, and a push to main has none. To close: write the prose for all seven pages (the four new Go files need real summaries, not a re-stamp), then run vault-check with --write and commit prose and lock together — re-stamping alone does not pass, the gate also requires the owning .md to appear in the diff. **DONE 2026-08-23, by another session, not this one** — `8037102 docs(vault): re-sync the 7 pages the merge train left stale` landed on main via PR #26, and `node scripts/vault-check.mjs` now reports "vault is up to date" at 95% coverage (94,203 / 98,666 lines, 226 files). The four Go files got real summaries rather than a bare re-stamp. The same train added **Rule #19** to CLAUDE.md, which is the actual fix for the recurrence: the sweep is now defined against the whole `origin/main...HEAD` diff rather than the last commit, because a gate that only looks at one commit is exactly what let four PRs merge inside 90 seconds and leave main red. Kept as a closed entry rather than deleted — the merge-order failure mode is worth being able to find again.
- [ ] P2 | 2026-08-23 | app/src/shortcuts.ts (DEFAULT_SHORTCUTS.find, .select_all) + app/src/SettingsModal.tsx (shortcuts tab) | TWO SHORTCUT ROWS ARE PLACEBO UI: `find` (Ctrl+F) and `select_all` (Ctrl+Shift+A) are stored, parsed into the table, and given recordable rows in Settings, but NOTHING calls `matches(e, sc.find)` or `matches(e, sc.select_all)` anywhere — grep the tree. There is no terminal SearchAddon at all; `FileEditor.tsx:307` has its own hardcoded Ctrl+F that never reaches the global handler, and select-all is only reachable from the terminal right-click menu (`terminalInstance.ts:496`). So the user can rebind either one and nothing happens, silently. Two ways to close it: wire `select_all` to the `terms.get(pid)?.term.selectAll()` the context menu already calls (about 4 lines, and it makes the row honest), and either add a real terminal search addon for `find` or hide its row and mark the field deprecated. Do NOT delete the Rust fields — that is a schema break for no gain. Found while making every other binding configurable in Phase 87; left alone because wiring new behaviour was outside that change.
- [ ] P1 | 2026-08-23 | app/src/InsightsClaudeCost.tsx + app/src/claudePricing.ts + app/src-tauri/server/internal/insights/claudeusage.go | **THE CLAUDE COST PANEL HAS NEVER BEEN RUN, AND ITS PRICE TABLE HAS AN EXPIRY DATE.** Two separate things to close. (1) A real run: open Monitor -> Claude on both a LOCAL and a REMOTE workspace and confirm the two produce the same shape. They are two independent implementations of one aggregation (Go for remote, Rust for local) which is exactly the setup where the two drift apart silently, so compare the same window on the same machine if you can. Check the scan cost against a real 240 MB `~/.claude/projects` at the 30d range: the mtime prune is what keeps it inside `curl --max-time 6`, and if it blows the timeout the fix is a per-file cache keyed by (path, mtime, size), NOT a smaller range. Cross-check one day against `claude -p /usage`'s "Last 24h - N requests" line; they measure different things but should not be wildly apart. (2) `claudePricing.ts` carries `PRICING_AS_OF` and a Sonnet 5 intro rate that EXPIRES 2026-08-31 - after that date the table silently falls back to the standard rate, which is correct, but nothing re-checks the rest of the table. Re-read it against published pricing (via the `claude-api` skill, NEVER from memory) whenever a model ships or a rate moves.
- [ ] P2 | 2026-08-23 | app/src-tauri/src/claude_log.rs | The dormant Phase 24.A log-mirror parser reads text/tool_name/timestamp and DISCARDS `message.usage` - the exact token counts Phase 84.E now reads straight off disk. Nothing is broken, but if that module is ever revived for the "unified view" it was kept alive for, it should surface usage rather than re-deriving it: the fields are already in the lines it parses.

- [ ] P1 | 2026-08-23 | app/src/InsightsAnalytics.tsx + app/src-tauri/server/internal/insights/analytics.go | **THE ANALYTICS TAB HAS NEVER BEEN RUN.** New endpoint + new panel, both landed unexecuted per Rule #17 (CI compiles, it does not run). To close: open the Monitor on a REMOTE workspace whose daemon has been up long enough to have history, switch to Analytics, and walk all four ranges. What to actually look at, in order of how likely it is to be wrong: (1) the 7d range - it is the whole reason the endpoint exists, and the failure mode is a chart that looks perfectly fine while covering the wrong window, so cross-check the axis labels against the range you picked; (2) the by_disk `growth` column - it comes from two correlated subqueries picking first/last `used` per mount, and a mount that appears mid-window will read as enormous growth; (3) the tooltip's clientX->viewBox conversion at a non-default panel width and in a floating window, not just the drawer; (4) RTL - the whole panel under Hebrew, where the numbers must stay LTR and the tables must not mirror their bar direction. Also confirm the local-workspace path shows the "needs the daemon" panel rather than an error, and that a workspace running an older daemon shows the 404 hint rather than a raw error string. And press **Copy for Claude** on a real report, paste it somewhere, and read it: the pure-function tests cover the formatting, but nothing covers `navigator.clipboard.writeText` actually being granted inside WebView2 for this panel, nor whether a 7d report is a sane size to paste (120 series rows).
- [ ] P3 | 2026-08-23 | app/src-tauri/src/insights_local.rs (insights_local_analytics) | Local workspaces get no Analytics at all - `insights_local` samples on demand and persists nothing, so `/analytics` answers `{"unavailable":"local"}` and the tab renders an explanation. Giving local parity means giving the desktop its own metrics store (a SQLite file under the config dir, a sampler on a timer, and the same 7-day sweep), which is a real feature, not a shim. Worth doing only if someone actually wants history for a local box; the Metrics tab already covers "what is it doing right now".
- [ ] P2 | 2026-08-23 | app/src-tauri/src/workspace_browser.rs (cleanup_workspace_sessions) | Deleting a workspace leaves its browser cookies behind on macOS. The cleanup deletes `<config>/browser-sessions/<ws>`, a directory nothing has written since Phase 62.A dropped the per-workspace `--user-data-dir`; on Windows the real state is the shared WebView2 profile and on macOS it is a `WKWebsiteDataStore` that has no cleanup path at all. Repro: open Browser in a workspace, log in to the tunneled service, delete the workspace, create a new one, open Browser — still logged in.
- [ ] P2 | 2026-08-23 | app/src-tauri/capabilities/default.json | LATENT: the capability is scoped to `windows: ["main"]`, and the workspace Browser child webview lives in the `main` window. Today the tunneled third-party page is denied every command only because its origin is `Origin::Remote` while the capability declares a `Local` execution context — not because no IPC reaches it (`__TAURI_INTERNALS__` IS injected into external URLs by Tauri 2.10.3). Adding a `remote` execution context to that file would hand a remote page the app's whole command surface. Not a bug now; a footgun with no guard rail. Commented at the top of `handle_ticket_navigation`.
- [ ] P1 | 2026-08-23 | app/src/{PaneTabs,AgentLight}.tsx, app/src-tauri/src/{lib.rs,rpc_server.rs}, hooks/claude-code.json | NOT VERIFIED LIVE: all of Phase 84 (tabs mode + the Claude traffic light). CI green is Rule #14 "compiles". Smoke steps — **tabs:** flip a 3-pane workspace to tabs, confirm each tab fills the area, that a `tail -f` in a background tab is still moving when you come back to it, Ctrl+Tab / Ctrl+1..9, `+` opens AND focuses the new tab, closing the middle tab lands on the right neighbour, dragging one tab onto another swaps them, flipping back to split restores the original ratios, an app restart remembers `tabs_mode`, and in Hebrew the strip does NOT mirror while the labels do. **Light:** `ymux setup-hooks --agent claude --source bundled`, then check `~/.claude/settings.json` has a `Notification` entry AND that `Stop`/`UserPromptSubmit` now point at a path that exists; prompt → green; a tool call in default mode → still green; finish → yellow; wait ~60s → red via `idle_prompt`; a ymux Gate card → red; close the pane → light gone; F5 the window → lights come back (proves `pane_agent_states`); and `ymux dev debug-log-tail` must show `type=`/`pane=` and NEVER `notification_text` (Rule #1).
- [ ] P2 | 2026-08-23 | hooks/claude-code.json + app/src-tauri/cli/src/hooks.rs (StopFailure) | THE TRAFFIC LIGHT'S ONE KNOWN LIE: a turn that dies on an API error shows **yellow "your turn"** instead of an error state, because `StopFailure` is not registered. Claude Code documents it as firing when the turn ends due to an API error, with a matcher on error type. Adding it means a new `PaneAgentState::Failed` arm, a fourth light (or a distinct shape on red), another `ymux_hooks_version` bump, and another forced `setup-hooks` re-run — which is why it is not in v1, not because it is unimportant.
- [x] P2 | 2026-08-23 | manifest.json:14 (hooks.claude-code.version) | STILL 1.1.0 WHILE THE SPEC IS 1.5.0. The desktop's outdated-hooks banner (updater.rs `check_remote_hooks`) compares the stamped `ymux_meta.hooks_version` against the MANIFEST, so until this moves nobody is ever told to re-run `setup-hooks` — and without that re-run the Phase 84.B red light never appears for anyone. Deliberately not bumped in the feature commit: that would banner every current 0.4.5 user before a CLI exists that can honour 1.5.0. **This belongs in the release checklist for whatever ships Phase 84.B** — add it to docs/RELEASING.md rather than leaving it here. **2026-08-23 Phase 86.E:** spec is now 1.6.0 (PreToolUse `timeout: 600`); manifest still 1.1.0, same reasoning. **DONE 2026-08-23 (Phase 86.E, pulled into scope by Yossi):** manifest bumped to 1.6.0. Safe now because the bootstrap rerun converges any install on the current CLI anyway (`ymux_entry_points_at` / `ymux_entry_has_timeout`); the banner is belt-and-braces for a user with `hooks.auto_install` off. A 0.5.0 CLI answering the banner writes 1.6.0 without `timeout`; the next connect with the new CLI adds it.
- [ ] P3 | 2026-08-23 | app/src-tauri/src/lib.rs (AppState.agent_runs) | Agent state is in-memory, so an app restart starts every light dark. That is the deliberate choice — restoring an eight-hour-old "running" would be a lie and the first hook restores the truth — but it means a workspace of idle agents shows nothing after a restart until each one is prodded. If that turns out to annoy, the fix is persisting with the state's timestamp and letting the existing 6h staleness cutoff discard anything old, NOT persisting the state alone.
- [ ] P3 | 2026-08-23 | app/src/components/PopoutTerminal.tsx:53 | A popped-out pane builds `new TerminalInstance(`popout-${sessionId}`)` with NO profile argument, so it silently falls back to `"local"`. A popped-out SSH pane therefore gets the local RTL profile — it will not pick up `force_rtl` (or any remote-profile setting) no matter what the user chose, and on a Hebrew-heavy remote it renders under the wrong assumptions about buffer order. Fix is to thread the owning pane's `profileFor(connection)` through to the popout; the awkward part is that the popout window does not currently carry the connection.
- [ ] P3 | 2026-08-23 | app/src/AgentLight.tsx | No `aria-live` announcement. A live region per pane would announce on every tool call, which is worse than silence; the right shape is ONE polite region for the focused pane only, announcing green→yellow→red transitions. Wants a screen-reader pass, not a guess.
- [ ] P1 | 2026-08-23 | app/src-tauri/src/lib.rs (pane_connect attach-only guard, list_workspace_tmux_sessions, session-owners.json) + app/src/PaneView.tsx | NOT VERIFIED LIVE: the whole TMUX-organization change. CI green is Rule #14 "compiles". Smoke, in this order: (1) a server with 3+ sessions across 2 directories, open a pane in a folder-anchored workspace — the picker opens on "This folder" and lists only those, the toggle shows the rest, and the count line matches; (2) Windows local workspace — open a pane, close it, open again: the zellij session shows as owned and appears under "This folder" even though zellij reports NO cwd; (3) THE BUG: a pane attached to a tmux session with `claude` running, open the connect wizard — the banner names the session, the command dropdown is greyed, Connect joins it and NOT ONE BYTE reaches the running Claude (`grep attach-only %APPDATA%\ymux\debug.log`); (4) same pane, "Start a new session instead" — a `<name>-2` session is created and the command runs there; (5) regression: a pane with no existing session — the command still runs, and lands INSIDE tmux rather than in the outer shell. **(6) added 2026-08-24, the foreign badge:** from workspace B, open Connect → "Whole server" and confirm a session created by workspace A wears a 📁 badge carrying **A's workspace name** (not its folder — the name proves the ownership path, the folder alone only proves the cwd path), that clicking it still attaches, and that switching to "This folder" shows NO badge on any row. Then the two silence cases, which are the ones that will actually be wrong if anything is: a Windows/zellij session with no ownership row must show **nothing at all** (zellij reports no cwd, so a badge here means the code guessed), and an unanchored workspace — one with no `cwd` — must show no badge on ANY row, since with no root there is nothing to be outside of.
- [ ] P2 | 2026-08-23 | app/src-tauri/src/lib.rs (list_workspace_tmux_sessions, pane_probe_tmux_sessions) | FIXED HERE, NOT VERIFIED ON A MAC: two ungated `list_zellij_sessions()` arms that shadowed the macOS local-tmux paths. Both returned unconditionally for any `Connection::Local`, so a Mac asked zellij (which is not installed there), got an empty list, and never reached its own tmux server — the local picker was silently empty and boot-time session restore skipped local panes. Now `cfg(windows)` / `cfg(not(windows))`. Pre-existing on `main` since 2026-08-19; found while extracting the shared list helper. Needs a real macOS run: a local tmux session must appear in the picker and survive an app restart.
- [ ] P3 | 2026-08-23 | app/src-tauri/src/lib.rs (pane_connect attach-guard) | The attach-only guard cannot ask an SSH host that has no live handle, and falls back to "not live" so a first connect still delivers its command. The frontend covers the real path (the wizard calls `workspace_ensure_connected` before `pane_target_session_state`), but a caller that reaches `pane_connect` directly — the RPC/CLI surface — on a cold SSH workspace can still inject into a session that was already running. Closing it properly means arming the handle inside `pane_connect`, which drags the auth-prompt path into a function that currently has none.

- [ ] P2 | 2026-08-23 | .claude/worktrees/ | TWO WORKTREES STILL LEFT IN PLACE (was three) BECAUSE ANOTHER SESSION WAS COMMITTING INTO THEM WHILE THE CLEANUP RAN — decide their fate when they are idle. **`mac-build-broken-b59d67` is DONE (2026-08-23): its work merged to `main` as 47dfab0, worktree removed and both branches deleted by the session that owned it.** Still open: `optimize-build-process-18f637` (and note the DIRECTORY NAME NO LONGER MATCHES ITS BRANCH: it now holds `claude/server-build-ci-pipeline-c0384e`, Phase 83) and `ymax-remote-server-direct-comm-eedd3b` (created mid-session, still at `ce50f0f`). The lesson that produced this entry stands: `mac-build-broken-b59d67` was on the approved delete list as "contained, zero unique commits" when the plan was written that morning and had grown three unique commits by the time deletion came around — Rule #16 in practice. Re-check containment IMMEDIATELY BEFORE deleting anything, never against a snapshot taken earlier in the session. And do not chase a count to zero: FOUR more worktrees were created by other sessions during the two hours this cleanup took (monitor-card-similar-screen, pane-tabs-status-indicator, workspace-tmux-organization, ymux-js-macos-issue), so the total went 6 -> 4 -> 7 while it ran. The sweep is a recurring chore against a moving target, not a one-off with an end state. **Count re-measured 2026-08-23: `git worktree list` reports SEVEN, not two — monitor-card-similar-screen-c5dbf5, nodejs-20-deprecation-1dec3e, pane-tabs-status-indicator-57b38c, workspace-tmux-organization-527c7e (detached), worktrees-branches-cleanup-7f7849 (holding `claude/valte-diff-validation-237bbd` — another name/branch mismatch), ymax-remote-server-direct-comm-eedd3b (detached, still ce50f0f) and ymux-js-macos-issue-98d625. Which is the entry's own point restated: the number is a moving target, so re-measure at delete time and never trust a figure written into this file.**
- [ ] P3 | 2026-08-23 | .claude/worktrees/{worktrees-clean-build-988849, mac-build-broken-b59d67} | TWO empty directories left on disk after `git worktree remove` — both deregistered from git (gone from `worktree list`) but the folders themselves are locked by a live process, so `remove` and `rm -rf` return "Device or resource busy" / "Permission denied". Zero files inside either. Harmless; they go away with a delete once nothing holds them. The second one is the clearer case: a session cannot remove its OWN worktree, because it is the process holding that directory as its cwd — git still deletes the contents and deregisters it, and only the final rmdir fails, so the operation looks like it failed while having mostly succeeded.

- [ ] P1 | 2026-08-23 | app/src-tauri/src/lib.rs (spawn_local_pty, pane_kill_session) + app/src/LocalSetupFlow.tsx | NOT VERIFIED LIVE: everything the macOS/0.5.0 merge touched. CI green is Rule #14 "compiles", and this merge rewrote the persistence path on BOTH platforms, so both need a real run. Windows: new workspace via the wizard, confirm the pane comes up inside zellij, that the session survives detach/reattach, and that Kill session reports `killed` (not `already_gone`) — `KillTarget` is now cfg-split and the Windows arm was re-gated by hand. macOS: the wizard shows the tmux group and nothing WSL, `InstallTmuxLocal` + `DeployTmuxConfLocal` run, and a local pane comes back after an app restart. Both: the workspace name defaults to "Local" on Windows and "tmux" on mac.
- [ ] P3 | 2026-08-23 | app/src/LocalSetupFlow.tsx:95, app/src/bindings/LocalSetupResult.ts | Naming debt the merge chose not to churn: `wslGroup` / `setWslGroup` now drive the macOS *tmux* group, and `LocalSetupResult.wsl_chain_ok` still names the field the UI stopped keying on — there is no WSL anywhere in this wizard any more. Renaming both is mechanical but crosses the ts-rs binding and the Rust struct, so it wants its own commit rather than riding along with a merge this size.
- [ ] P3 | 2026-08-23 | app/src/LocalSetupFlow.tsx:92 | `wslUsername` is still seeded from the default distro and sent as `wsl_username`, but no input sets it — the release line dropped the field with the WSL group and the merge kept that. Dead in practice on both platforms. Delete it with the rename above, or give it back a home if `local_setup_start` still needs the value.

- [ ] P1 | 2026-08-20 | 322655e — app/src/terminalInstance.ts (flushPending, normaliseIncomingToLogical, applyRowDirections, bufferIsVisualOrder) + app/src/copyBidi.ts (visualToLogicalStream, ANSI_RE export) | **LOCAL HEBREW: BETTER DISPLAY vs TYPING LATENCY — A REAL TRADE-OFF, BOTH SIDES CONFIRMED LIVE BY YOSSI. THE COMMIT TO INVESTIGATE IS `322655e feat(rtl): normalise Claude's visual output to logical on the way in` (2026-08-20 16:53, the LAST commit on the zellij chain).** What it does, from its own commit body — the rule is three rows wide and only one row is the target:
    buffer   render          letters  placement
    visual   dir="rtl"       wrong    RIGHT
    visual   bidi-override   RIGHT    left      <- what shipped before it
    LOGICAL  dir="rtl"       RIGHT    RIGHT     <- 322655e, and what remote has always done
Placement can only come from the browser under `dir="rtl"`, and the browser only gets the letters right if handed LOGICAL text — so the stream is converted on the way in and a local pane stops being a special case. **LIVE EVIDENCE (2026-08-20, Yossi, real panes, three builds):** (1) merged 0.5.0 WITH 322655e — Hebrew in a local pane displayed *noticeably better* than any earlier build, and typing lagged; (2) main-only 7382de9 — no lag, Hebrew display visibly worse (it has none of the 18 RTL fixes and no `terminal.rtl` split at all); (3) branch build `ymux-rtl-test` (mtime 2026-08-19 16:32, end of `zellinj` at 37c6bc9, predates 322655e) — no lag. So the better display and the lag are the SAME commit. **COST:** `normaliseIncomingToLogical` = `bidiOwnedByTui && rtlModeRendered == "auto_per_line"`, and `default_local_rtl` ships `tui_owns_bidi: true`, so every local pane routes each rAF flush through `visualToLogicalStream(merged)`: an `RTL_RE` scan, `chunk.matchAll(ANSI_RE)` over CSI/OSC-dense text, then split/map/join per run. `!RTL_RE.test(chunk)` early-outs an all-Latin pane, which is why only Hebrew panes pay. It does NOT show up as a blocked main thread — zero `UI stall` warnings and one `longtask` across a 9-minute session — the cost is spread thinly across flushes. **CURRENT STATE ON YOSSI'S MACHINE:** `terminal.rtl.local.rtl_mode` set to `"off"` (WebGL verbatim), `tui_owns_bidi` left `true`. No lag, worse display. This is exactly the fallback the commit body names: "the fallback if the caret misbehaves is rtl_mode='off' — WebGL verbatim, which is precisely the old behaviour". Remote is untouched at `auto_per_line` / `tui_owns_bidi:false`. **TO REPRODUCE EITHER SIDE:** set `terminal.rtl.local.rtl_mode` to `auto_per_line` for the good-display-with-lag state, `off` for the no-lag state — one field, live, no rebuild (`ymux settings set --key terminal.rtl.local.rtl_mode --value auto_per_line`). **WHAT THE COMMIT ITSELF FLAGS AS UNTESTED:** "Not run live: the caret while typing Hebrew is the known risk and only a real pane can answer it." The latency is what a live run actually found; the caret question is still open and should be checked in the same session. **DIRECTIONS WORTH TRYING, none validated:** cache the RTL verdict per pane instead of re-scanning every chunk; narrow the normalise to panes actually running Claude rather than every local pane; make the reorder incremental rather than whole-chunk. Do NOT just revert 322655e — it is the only one of the three rows that gets letters AND placement right, and reverting returns local panes to "correct letters pinned to the left edge".
- [ ] P2 | 2026-08-20 | app/src-tauri/src/lib.rs (parse_zellij_sessions) | A ZELLIJ SESSION HAS NO `auto_name` AND NOBODY HAS LOOKED AT WHAT THE PICKER SHOWS. The merge of the zellij chain into 0.5.0 broke the build on exactly one line: `parse_zellij_sessions` is new on that branch, was written before Phase 81.F added the field, and so omitted it from the `TmuxSessionInfo` initializer. Patched as `auto_name: None`, which compiles and is honest — zellij sessions carry no session-meta join, so the picker falls back to the raw name the same way a pre-rename server does. What is NOT known: whether a zellij session that IS running Claude should get a name, and where from. tmux gets it from `~/.ymux/session-meta.json`, written by the CLI hooks; nothing wires that to zellij. Decide before the release notes claim session naming works everywhere. Found by `cargo check`, not by a test — there is no test covering the zellij picker's display fields.
- [ ] P3 | 2026-08-23 | .github/workflows/build-macos-intel.yml:112 | Mac builds ship AD-HOC SIGNED BY DECISION, not by omission — Yossi declined the Apple Developer membership 2026-08-23 (docs/DECISIONS.md, Decided). Kept only as the warning for whoever reverses that: the developer-id path in this workflow has never executed once, so notarytool, stapling and the `spctl rejected a Developer ID signed build` branch all run for the first time on the build that follows the secrets landing. Budget a failed run or two and do not schedule it against a release cut. Nothing to do until then; the README carries the user-facing `xattr` step.
- [ ] P3 | 2026-08-23 | .github/workflows/build-windows.yml:74-84 (the `actions/cache` step) | THE `tauri-bundler-toolchains` CACHE LOOKS LIKE IT HAS NEVER WORKED. It caches `app/src-tauri/target/release/{wix,nsis}`, but run 32651903558 (workflow_dispatch from main, the post-#19 verification build) missed on both the key AND the `tauri-bundler-toolchains-Windows-` restore-key, then saved an entry of **25,922 bytes** — far too small to be a WiX or NSIS toolchain. In that same run the bundler logged `Info extracting NSIS` and `Info Verifying NSIS package`, i.e. it downloaded them itself. Reading together: the bundler puts the toolchains somewhere other than those two paths (newer Tauri uses a per-user cache dir, not `target/`), so the step archives near-empty directories and the download the comment says it saves is paid on every run anyway. NOT caused by the v4->v6 bump in PR #19 — the paths and key are unchanged from v4, and the repo's cache list holds no older `tauri-bundler-*` entry to compare against. REPRO / CONFIRM: dispatch build-windows twice in a row on an unchanged `app/package-lock.json`. The second run should report a cache hit; if `Info extracting NSIS` still appears in it, the cache is confirmed dead weight. FIX is then either to point `path:` at the real download location or to drop the step. Cost today is only the ~30s download plus a stale 25KB cache entry, hence P3.
- [ ] P3 | 2026-08-23 | app/src-tauri/crates/ymux-ssh/src/lib.rs:339 | `try_authenticate` step 3 resolves the home dir with `env::var("USERPROFILE").or_else(HOME).map_err(...)?` — a missing home dir aborts the WHOLE auth ladder before step 4 (password), instead of skipping the step that needs it. Repro: run with both USERPROFILE and HOME unset and connect to a password-only host — you get an env-var error, never a password prompt. No shipping target of ours leaves both unset (Windows/macOS always set one), so this is correctness rather than a live bug; found by reading `bouhady/winmux`'s Android fork, which hit it for real (Phase 82.A, commit 1ea489d) and fixes it by matching instead of `?` and skipping the step — there are no default keys to scan without a home dir anyway.
- [ ] P2 | 2026-08-18 | app/src-tauri/resources/remote-manifest.json | Phase 81.F changed CLI source, so the Linux CLI binary's sha256 genuinely changes — this is the case Rule #12 carves out ("discard the churn UNLESS the embedded SHA actually changed"), and ci-windows run 32174489370 flagged the drift as expected. The committed manifest still claims `e50c2ceb…`. It can only be regenerated by a real `build-linux-cli.ps1` run on Windows, so it must be refreshed on the release cut that ships this — a hand-edited sha would be worse than a stale one, since sha256 IS the bootstrap comparison.
- [ ] P1 | 2026-08-18 | app/src-tauri/src/{lib.rs,rpc_server.rs}, app/src/PaneView.tsx | NOT LIVE-VERIFIED: the desktop half of Phase 81.F/81.G (session `auto_name` + the hook pane resolution). 81.G fixed the two defects Yossi's install exposed (stale-env pane id, and auto_name erased by a lost read-modify-write) and both fixes are unit-tested + CLI-smoked, but the DESKTOP side of the pane resolution has still only been compiled. The decisive test needs a real Windows box + server: a tmux session that OUTLIVED its creating pane (re-attach an orphan session to a fresh pane) must still get its header named - that is the exact case the stale WINMUX_PANE_ID broke. Also check the debug.log now says `hook pane recovered via tmux` rather than nothing at all.
- [ ] P3 | 2026-08-18 | app/src/App.tsx:1058 | The OS window title reads only the manual pane `title`, never `auto_title` — so a Claude-named / auto-named pane falls through to the connection string in Alt+Tab, inconsistent with the pane header right next to it. Pre-existing Phase 81 oversight, widened in reach by 81.F (more panes now carry an auto name). One-line fix, but it changes what the taskbar shows, so it wants a deliberate call rather than a drive-by.
- [ ] P2 | 2026-08-19 | app/src-tauri/Cargo.toml (no tauri-plugin-single-instance) | TWO ymux.exe INSTANCES STILL RACE ON LOG ROTATION. Phase 80.D made debug.log writes single-owner WITHIN a process, and O_APPEND keeps a single write_all atomic even across processes, so lines from two instances interleave cleanly rather than tearing. Rotation does not: both would stat, both would rename, and one clobbers the other's debug.log.1. Out of scope for an in-process owner. This is the same class as the P2 above about two builds sharing %APPDATA%\ymux, and the same fix would cover both: a single-instance plugin, or a lock file naming the holding pid.
- [ ] P2 | 2026-08-19 | app/src-tauri/src/claude_usage.rs:262 | BACKOFF PATH LOGS ON EVERY REJECTION -> LOG FLOOD. `debug.flooded-2026-08-18.log` is 1.6MB of which 15,477 lines are the single message `[DEBUG] [USAGE] workspace=<id> backoff`. in_backoff() correctly suppresses the WORK but not the LOG, and the caller retries in a tight loop, so the throttle produces more I/O than the thing it throttles. Fix: log the entry into backoff once (with the until-timestamp) and stay silent while it holds, or rate-limit the line.

- [ ] P3 | 2026-08-19 | every `app/scripts/*.ps1` | **A NON-ASCII CHARACTER INSIDE A POWERSHELL STRING LITERAL BREAKS THE BUILD, SILENTLY AND CONFUSINGLY.** Windows PowerShell 5.1 reads a BOM-less UTF-8 file as ANSI. A UTF-8 em-dash (`E2 80 94`) decodes to three CP1252 chars whose last one is U+201D, a smart quote the PowerShell tokenizer accepts as a string delimiter -- so `Write-Host "... - left in place"` with a real em-dash terminates the string in the wrong place and the parser then reports a cascade of `Missing closing '}'` errors pointing at innocent lines 10-100 lines away. Hit while editing `build-linux-cli.ps1`; the existing em-dashes in that file are all inside `#` comments, which is why it had never bitten. **GUARDED 2026-08-19:** `ci-windows.yml` now runs a "Parse-check the build scripts" step (`shell: powershell`, i.e. 5.1) over `app/scripts/*.ps1` and fails on any parse error, in seconds, before anything expensive runs. Left open as P3 because the guard catches it rather than preventing it -- writing the scripts with a UTF-8 BOM, which is what WinPS 5.1 actually wants, would.
- [ ] P2 | 2026-08-18 | app/src-tauri/src/workspaces_merge.rs | TWO YMUX BUILDS SHARING %APPDATA%\ymux — was P1, now largely handled. Hit live: winmux-beta3-MEGA-test.exe ran beside a dev build; it predates parent_id / is_project_root, serde ignored the unknown keys and save_to_disk rewrote the WHOLE file without them, erasing the workspace nesting with nothing in the new build's log. FIXED FORWARD by a three-way merge on every save: re-read the file, and if it moved since we last touched it apply only our delta on top of theirs. Merging at the JSON level (not over WorkspacesFile) means fields a binary has never heard of survive its own save, which is the actual class of bug. WHAT IS STILL OPEN: (1) an already-shipped older binary has none of this, so it will keep stripping until it is replaced — YMUX_CONFIG_DIR isolation is the only defence there; (2) the merge path has NOT run in a real app — an idle winmux never saves, so it is covered by 12 tests at the `reconcile` seam rather than live; (3) a lock file naming the holding pid would still be worth having, to say out loud that two instances share one config instead of silently reconciling behind the user's back.
- [ ] P2 | 2026-08-18 | app/scripts/build-linux-cli.ps1 (RUSTFLAGS scrub) + .github/workflows/ci-windows.yml (Report staged-resource drift) | THE `ymux-linux-x64` CI BUILDS IS NOT THE ONE COMMITTED, AND THE ALARM FOR IT IS ALWAYS ON. CI's fresh build came out 2,498,992 bytes / sha fbf2c01c… against the committed 2,521,136 / 8ecb0623… — a 22KB difference, i.e. a different compiler, not a path string. Local rustc here is 1.95.0; CI takes `dtolnay/rust-toolchain@stable`. PRE-EXISTING, not the rename: run 32178442186 (the merge commit, before any of this work) emitted the same `##[notice]remote-manifest sha256 changed`. (2026-08-23: that pin is now the SHA of the action's `stable` branch, PR #19. Form only — it still installs current stable Rust, so this entry is untouched and still wants a `rust-toolchain.toml`.) Nothing breaks at runtime — a release regenerates the binary and remote-manifest.json together, so the sha the bootstrap compares against is always self-consistent. Two real costs though: build-linux-cli.ps1's header claims the RUSTFLAGS scrub makes the output "byte-identical regardless of who built it", which is not true across toolchain versions; and the CI notice fires on EVERY run, so it has stopped being a signal — it was written to flag the case where the staged CLI actually moved. Fix is one of: pin the toolchain (rust-toolchain.toml, so CI and Yossi's box agree), or stop committing the binary and let the release build produce it. **UPDATE 2026-08-19:** the *second* cause is fixed — `cli/build.rs` baked `SystemTime::now()` into `YMUX_BUILD_TIME`, so two builds of identical source on the SAME machine also disagreed; it is the commit time now. The cross-toolchain 22KB delta is untouched and still needs `rust-toolchain.toml`. The always-on CI notice should also quieten down: `build-linux-cli.ps1` no longer rewrites `remote-manifest.json` (or restages a binary) when the sha256 hasn't moved, so the `git diff` drift step now fires only on a real change.
- [ ] P2 | 2026-08-20 | app/src/terminalInstance.ts (contextmenu handler ~:917, showTerminalContextMenu ~:464) | **RIGHT-CLICK Copy stopped working on a local zellij pane after `mouse_mode true`.** Reported by Yossi in the live pass: wheel scroll works, Shift+drag copies, Ctrl+C copies — right-click does not. Not a regression in the copy plumbing (all four paths share `copyToClipboard` now and the other three work); it is a consequence of handing zellij the mouse, and there are two independent causes, both plausible and both needing a live check before fixing: (1) with mouse reporting on, xterm.js forwards the right-click to zellij as an SGR event and our `contextmenu` listener may never fire, so the menu never opens; (2) even if it opens, `showTerminalContextMenu` builds its Copy item from `ti.term.getSelection()`, which is EMPTY when the selection belongs to zellij rather than to xterm.js — so the item renders disabled (`!!sel`). Shift bypasses zellij's capture, which is exactly why the Shift paths are the ones that work. Fix options, in rough order of cost: require Shift+right-click and say so in the menu; or have the menu fall back to the last OSC 52 payload when xterm has no selection; or ask zellij for the selection. Deprioritised by Yossi — "less important for now".
- [ ] P2 | 2026-08-19 | app/src/terminalInstance.ts (renderer fork, ~line 791) | A LOCAL Windows pane renders Hebrew REVERSED under `rtl_mode="auto_per_line"`, even though the stream reaching xterm there is logical order (proved: `bidi_reorder`, which does logical→visual, renders it correctly on the same pane). auto_per_line uses the DOM renderer + per-row `dir`, so the browser should reorder it correctly — the same setting works on an SSH pane. Something on the local path is not getting the per-row dir treatment, most likely the pane being built with the WebGL renderer (`rtlModeAtConstruct` frozen from a different mode at construction). NOT diagnosed, and deliberately not guessed at: the next step is a log line stating, per pane, which renderer was chosen and what `rtlModeAtConstruct` was, then read `%APPDATA%\ymux\debug.log`. **2026-08-19 (later): that log line now EXISTS** — `rtl-pane` at construction (profile / live mode / rtlModeAtConstruct / renderer actually loaded / policy / staleRenderer) and `rtl-dirs` per direction pass. Reproduce and read those two lines; the diagnosis should fall out of them rather than out of a screenshot. Worked around, not fixed, by the local/remote profile split (`0b40012`): local defaults to `bidi_reorder`, which is correct there. Repro: local PowerShell pane, rtl_mode=auto_per_line, restart the app, `echo "שלום"` → shows `םולש`. **2026-08-20, much stronger evidence:** Yossi's side-by-side shows the two panes are exactly inverted — plain PowerShell displays reversed but pastes CORRECTLY, Claude Code displays correctly but pasted reversed. The clipboard half is now fixed (see the 2026-08-20 entry above), which isolates this one precisely: PowerShell's buffer is LOGICAL (proved by its copy being right), so this is purely a rendering bug — auto_per_line's DOM renderer plus a per-row `dir` should lay logical Hebrew out correctly and does not. Same visual/logical model as the copy bug, opposite sign; they are siblings.
- [ ] P2 | 2026-08-19 | app/src/App.tsx (connectPane + the feed:item-added listener) | **Re-attaching to a persistent session where Claude never stopped starts in the WRONG bidi state until the first hook fires.** Measured from Yossi's debug.log: panes with Claude running in them reported `tui=0`, because a reattach produces no `mode=claude` (the wizard was not used) and no `session-start` (Claude never started — that is what persistence means), and the title never matches (`title-seen ... match=0 len=57`, zellij's own). Mitigated, not fixed: ANY Claude hook now sets the signal, and `stop` lands after every reply, so the state corrects itself on the first interaction. The residue is the window between reattach and that first hook — the exact moment a user opens the app and looks at Claude — plus the case where the ymux Claude hooks are not installed at all, where nothing ever fires. Real fix is to PERSIST the flag with the pane (workspaces.json, `#[serde(default)]`), so a persistent session carries 'Claude is running in me' the way it carries everything else. Trade-off to write down when doing it: a stale true (Claude died while the app was closed) makes SHELL Hebrew render reversed until a hook or a title corrects it — the same failure class, inverted.
- [x] P1 | 2026-08-19 | app/src-tauri/cli/src/hooks.rs (is_ymux_entry / the skip path) | **`setup-hooks` decides an entry is 'already present' by NAME and never checks that the command still exists, so the winmux -> ymux rename left a permanently dead hook.** From Yossi's Claude pane, verbatim: `Stop hook error: /usr/bin/bash: line 1: C:\Users\mlastudent371\winmux-cli.exe: No such file or directory`. His ~/.claude/settings.json references BOTH spellings; `is_ymux_entry` correctly matches the old one as ours, and the installer then reports `already present (would skip)` and leaves the dead path in place. Verified here: `--dry-run` skips all four, `--force` rewrites all four with the live path and takes a timestamped .bak. Two costs, and the second is the expensive one: the hook fires and errors on EVERY Claude stop, and it silently disabled the hook-based half of the per-pane 'Claude is in front' signal that local RTL depends on -- which is why local hooks never reached the app all session while remote ones did, and why that looked like a detection bug for several rounds. Fix: in the skip path, resolve the entry's executable and treat a missing file as needing replacement, so 'already present' means 'present AND runnable'. Workaround until then: re-run with `--force`. **DONE (found closed 2026-08-23, Phase 86 audit):** `ymux_entry_is_runnable` (hooks.rs:615, called at :390) already repairs an entry whose executable is missing; the entry was never ticked.
- [ ] P1 | 2026-08-19 | app/src/textDirection.ts (nextTuiOwnsBidi) + app/src/terminalInstance.ts (onTitleChanged) | **`tui_owns_bidi` IS DEAD CODE IN PRACTICE — the detector never fires.** Proved from Yossi's debug.log on the build that added the RTL diagnostics: every `rtl-dirs` line reports `tui=0`, on every pane, both profiles, and the whole file contains not one `tui-owns-bidi` transition. The detection reads the terminal TITLE (OSC 0/2), and inside zellij the title reaching ymux is zellij's own — Claude's never gets through. Over SSH+tmux it did not fire either. So the Settings checkbox promises behaviour that cannot happen, and the dynamic "no bidi while a self-reordering TUI holds the pane" switch has never actually run. Worked around, not fixed: local now defaults to `rtl_mode="off"`, which needs no detection — at the cost of shell Hebrew (logical order) rendering reversed on a local pane. The fix is a signal that survives a multiplexer: **the ymux Claude hooks already run per pane with `YMUX_PANE_ID` set** and could report Claude's start/stop directly, which is both more reliable than a title and works over SSH too. Until then, do not add features that depend on `tuiOwnsBidi` being true.
- [ ] P2 | 2026-08-19 | app/src/bidi.ts + app/src/terminalInstance.ts (flushPending) | Under `rtl_mode="bidi_reorder"` the caret stays pinned to the right instead of tracking the text, and a partial repaint can leave a line half-reordered ("part rewritten correctly and part in the middle rewritten again"). Both are inherent to transforming the byte stream for a TUI that repaints regions and positions its cursor in columns of the UNtransformed text — two coordinate systems that diverge. This is what terminal-wg calls "the cursed cursor"; no xterm.js-based terminal has solved it. A real fix needs whole-line reassembly before reordering plus cursor tracking through the transform, and is its own piece of work — not a follow-up to squeeze into an RTL bugfix. See docs/DECISIONS.md, the 2026-08-19 entries.
- [ ] P2 | 2026-08-19 | app/src-tauri/crates/ymux-core/src/lib.rs (config_dir + write_line) | `cargo test` WRITES INTO THE USER'S REAL `%APPDATA%\ymux\debug.log`. Proof: while diagnosing a live RTL run I read the log tail and found the newest 41 lines were TICKETS parse warnings and four repeats of "migrated 1 WSL connection(s) to local" — those are unit tests, not an app run. `config_dir()` honours `YMUX_CONFIG_DIR`, so the tests CAN be isolated; most of them just don't set it. Two costs: the user's support log gets salted with test noise, and it burns real diagnostic time (it sent me looking for an app boot that had never happened). Fix: set `YMUX_CONFIG_DIR` to a temp dir for the whole test binary (a `#[ctor]`-style once, or a test-only default in `config_dir` gated on `cfg!(test)`).

- [ ] P2 | 2026-08-18 | app/scripts/build-linux-cli.ps1 (RUSTFLAGS scrub) + .github/workflows/ci-windows.yml (Report staged-resource drift) | THE `ymux-linux-x64` CI BUILDS IS NOT THE ONE COMMITTED, AND THE ALARM FOR IT IS ALWAYS ON. CI's fresh build came out 2,498,992 bytes / sha fbf2c01c… against the committed 2,521,136 / 8ecb0623… — a 22KB difference, i.e. a different compiler, not a path string. Local rustc here is 1.95.0; CI takes `dtolnay/rust-toolchain@stable`. PRE-EXISTING, not the rename: run 32178442186 (the merge commit, before any of this work) emitted the same `##[notice]remote-manifest sha256 changed`. Nothing breaks at runtime — a release regenerates the binary and remote-manifest.json together, so the sha the bootstrap compares against is always self-consistent. Two real costs though: build-linux-cli.ps1's header claims the RUSTFLAGS scrub makes the output "byte-identical regardless of who built it", which is not true across toolchain versions; and the CI notice fires on EVERY run, so it has stopped being a signal — it was written to flag the case where the staged CLI actually moved. Fix is one of: pin the toolchain (rust-toolchain.toml, so CI and Yossi's box agree), or stop committing the binary and let the release build produce it.
- [ ] P2 | 2026-08-18 | sdk-gen/specs/openapi.json, sdk/typescript/src/types.gen.ts | THE RENAME COMMIT ALSO CARRIES SDK DRIFT THAT PREDATES IT. Running `sdk-gen/ci-check.mjs` regenerated the specs and found ~400 lines of openapi.json and ~270 of types.gen.ts that were already stale — new components (`HookDecisionRequest`, `HookForwardRequest`) from an earlier server change where nobody ran `npm run gen`. The regenerated output is by definition correct, so it was committed rather than left stale, but it is NOT mine and reviewing the rename diff will show it as unexplained. Root cause worth fixing: `ci-windows.yml` does not run the drift guard, which is why it went unnoticed — add a `node sdk-gen/ci-check.mjs` step and this class of surprise stops recurring. ROOT CAUSE FIXED 2026-08-23 (Phase 83): ci-windows.yml now runs the drift-guard on every push/PR. What is still open here is only the review of the ~670 lines that rode in on the rename commit — they are correct by construction (regenerated output) but nobody has read them.
- [ ] P2 | 2026-08-18 | build-and-verify.ps1:23,63 | LEFT ON THE OLD NAMES ON PURPOSE — CLAUDE.md Rule #11 puts repo-root .ps1 helpers off limits. The rename sweep did touch it and was reverted, which was right twice over: both strings name things that really are still called winmux on Yossi's disk (the worktree `winmux-wt-lh-insights` and `C:\Users\mlastudent371\winmux-beta3-lh-insights-test.exe`), so renaming them inside the script would have BROKEN it rather than updated it. Yossi's to change, if and when he renames those two on disk. `app/scripts/*.ps1` are NOT in this category — they are project build scripts and were renamed normally.
- [ ] P1 | 2026-08-18 | crates/ymux-tunnel/src/lib.rs (CHALLENGE_TAG), server/internal/chat/chat_hookrpc.go (challengeTag) | RETIRE THE RENAME SHIMS ONE RELEASE AFTER 0.5.0. The handshake still EMITS the legacy `WINMUX-CHALLENGE` tag, because a pre-rename `winmux-linux-x64` does a literal prefix match and hangs up on anything else — so until every provisioned remote has been re-bootstrapped, ymux↔ymux traffic is still speaking winmux on the wire. Both ends already read and mirror either dialect (covered by TestHookHandshakeAcceptsBothDialects / TestHookHandshakeDeniesInClientDialect), so the flip is a one-line change on each side — do them together. Same release should drop: the `WINMUX_*` dual-write (lib.rs spawn_local_pty + the tmux set-environment loop, ymux-tunnel write_remote_env_file, Go spawnEnv), the CLI's `adopt_legacy_env` + `migrate_legacy_home_dir`, the legacy named-pipe listener (rpc_server.rs LEGACY_LISTENER_POOL_SIZE + ymux-core `pipe_name_legacy`), `X-Winmux-Truncated`, and the `use_winmux_tmux_config` serde alias. Keep the `%APPDATA%` and `~/.winmux` migrations longer — those cost nothing and a user can upgrade from any age.
- [ ] P1 | 2026-08-18 | whole rename, no single file | NOTHING ABOUT THIS RENAME HAS BEEN VERIFIED LIVE (Rule #14). Green: cargo check + cargo test, go vet + go test (incl. 2 new dual-dialect handshake tests), tsc, vite build, and all three embedded binaries rebaked from renamed sources. NOT run: the app itself. Smoke list, in order, and each one exercises a shim that only exists on the upgrade path: (1) launch with an existing `%APPDATA%\winmux` present and confirm the log line `config dir migrated … (winmux -> ymux rename)` and that workspaces/settings/keys all survived; (2) connect to a server provisioned by the OLD build and confirm `bootstrap: migrated remote …` plus a working pane — this is the `~/.winmux` → `~/.ymux` copy AND the legacy handshake tag together; (3) fire a Claude hook in that pane and confirm the permission card appears (proves env promotion + `is_ymux_entry` replacing rather than duplicating the old hook — if it DUPLICATES you get two cards per tool call); (4) Add-ons → Insights on a host with the old daemon: detect must report the existing install, and after Update `systemctl --user list-units | grep -c mux` must be 1, not 2; (5) `ymux list-workspaces` from a shell, then the same with a pre-rename `winmux-cli.exe` if one is still on PATH (proves the legacy pipe listener).
- [ ] P3 | 2026-08-18 | app/src/PaneView.tsx:1794 | The new-connection wizard renders its OWN copy of the remote directory list (`.nc-browse-*` / `.nc-dir-item`, ncView()==="browse") — same `file_list_remote` call, same navigation, different chrome. The standalone copy was extracted to app/src/DirPicker.tsx (it had been dead code: its guard `dirPicker() && !dirPickForNewConn()` could never be true); the wizard copy was left alone deliberately, since folding it in means touching the connect flow. Two implementations of one browser will drift. Fix: give DirPicker an `inline` variant and have the wizard render it.
- [ ] P1 | 2026-08-18 | app/src/Sidebar.tsx + app/src-tauri/src/lib.rs | NOT VERIFIED LIVE: project folders v4 (a project folder IS a child workspace) and the pane-header overflow menu. Supersedes the 2026-08-16 v2 entry, whose model no longer exists. Smoke: (1) split to 6-8 panes, shrink the window, buttons move into the chevron menu and the menu OPENS - check Hebrew too; (2) right-click an SSH workspace -> pin -> the SFTP browser lists REAL remote dirs -> pick a repo -> a child row appears nested under it with a folder glyph; (3) select it, open a pane -> the wizard shows the folder read-only, TMUX/regular, and a session list containing ONLY that folder's sessions -> connect -> `pwd` is the folder (this exercises the SSH-cwd injection); (4) expand its worktrees -> open one -> nests under the folder, `pwd` is the worktree, and the repo root's own entry does NOT appear as a stub; (5) + -> new worktree -> its workspace opens AND SURVIVES (the cmux#5032 trap); (6) delete the parent -> the confirm NAMES the descendants and the live count -> all of them go, directories on the host untouched; (7) disconnected host -> clear error, no stuck spinner; non-repo path -> git's message, nothing created; (8) restart -> nesting + collapse state persist; (9) MIGRATION: open a workspaces.json still carrying `project_folders` and confirm the pins reappear as child workspaces and the worktree workspaces re-parent under them.
- [ ] P1 | 2026-08-18 | app/src/App.tsx (reconnect) | NOT VERIFIED LIVE: the per-pane reconnect fix. Compiles + tsc clean, but the decisive test is 4 panes on one workspace surviving a single full network drop (all four must retry and reattach; before the fix three were abandoned without a single attempt). There is no JS test runner in this repo, so this cannot be covered by a unit test without adding one — it needs a real build and a real drop.
- [ ] P2 | 2026-08-20 | app/src-tauri/src/provisioning.rs:285 | `save_workspace_secret` is write-only: nothing in the tree reads provisioning-secrets.json back — there is no `dpapi_unprotect` and no loader. So on Windows it stores a DPAPI blob nobody consumes, and 82.D made it a no-op elsewhere. Either give it a reader (the "remember this password" feature it was groundwork for) or delete it and the file. Deciding this is what gates a real Keychain integration on macOS.
- [ ] P3 | 2026-08-20 | app/src-tauri/src/updater.rs | No in-app update path on macOS. Needs .dmg/.app.tar.gz asset selection in `parse_releases` (it matches `-setup.exe` only), plus mount-and-swap of a running .app and a relaunch. 82.D made the Windows-only path refuse cleanly instead of quitting the app; this is the feature that would replace the refusal.
- [ ] P3 | 2026-08-20 | app/src-tauri/tauri.conf.json | `mainBinaryName` is unset, so the macOS bundle's executable is `ymux.app/Contents/MacOS/app` — the cargo package name, not the product. Found because the Phase 82.E assert hardcoded `MacOS/ymux` and failed; the step globs now, but the underlying wart is real: macOS shows the process as "app" in Activity Monitor, the force-quit dialog and any crash report. Setting `mainBinaryName: "ymux"` fixes it but renames the bundled binary, so it wants its own commit and a fresh CI run rather than riding along with something else.
- [ ] P3 | 2026-08-20 | app/src-tauri/tauri.macos.conf.json | No macOS signing / notarization / entitlements config (no `bundle.macOS` signing block, no hardened runtime). A distributed .dmg is Gatekeeper-quarantined — the user must right-click-Open or clear the quarantine bit by hand. Needs an Apple Developer ID before it can be done at all.
- [ ] P3 | 2026-08-20 | app/src-tauri/src/lib.rs (~90 sites) | `lock().unwrap()` on the state mutexes throughout, against Rule #4. NOT new and NOT macOS-specific — I first flagged it as arriving with the mac local-workspace flow, then counted: it is the house pattern for `state.core.sessions` / `pane_sessions` / `settings` on every transport. Low risk (a poisoned mutex there means a handler already panicked, i.e. the app is down) but it is the rule's plain text, and it is a single mechanical sweep rather than a judgement call. Do it as its own commit, not folded into a feature.
- [ ] P1 | 2026-08-18 | app/src-tauri/src/bootstrap_guard.rs | NOT VERIFIED LIVE: bootstrap dedup + negative cache + skew banner. Unit-tested (6 tests) but never run against a real host. Repro that matters: build from main (manifest e50c2ceb…) against a server holding b3a3974…, open 3-4 panes at once, expect EXACTLY ONE SFTP stream in debug.log, one `bootstrap: COMPLETE — upload verified`, and no `.tmp` left in ~/.ymux/bin.
- [ ] P2 | 2026-08-18 | remote server ~/.ymux/bin | Stale temp files left by the OLD code on Yossi's server: `ymux-linux-x64.tmp` (522,240 bytes = exactly 2 x russh-sftp's 261,120 chunk) and `ymux-server.tmp` (0 bytes). Nothing cleans these up — the new code removes its own partials but has no sweeper for pre-existing ones. Needs Yossi's explicit go-ahead to delete on his box.
- [ ] P2 | 2026-08-11 | app/src-tauri/src/lib.rs:1832 | The tmux attach script appends `\; set -g mouse on`, which directly contradicts `set -g mouse off` in resources/ymux-tmux.conf:28. That conf line is deliberate and its comment says why: with mouse on, tmux captures every click via xterm mouse-tracking, so drag-to-select and right-click-to-paste stop working inside tmux panes. Phase 65 (bug EE) turned it back on via the command chain to get tmux-native wheel scrollback, because `mouse on` IN the conf garbled Claude Code's output. Net effect today: mouse is on, so selection/paste-by-mouse inside tmux is degraded on BOTH ssh and wsl panes, for scrollback. Needs a decision, not a flip — possibly per-pane or tied to whether an alt-screen app is running.
- [ ] P2 | 2026-08-11, re-diagnosed 2026-08-18 | app/src/terminalInstance.ts (~17 sites) | Raw `console.*` throughout, against Rule #9 (frontend must use `createLogger`). This is how the clipboard-read failure stayed invisible: the only handler was `console.warn("terminal paste failed")`, which never reaches %APPDATA%\winmux\debug.log. **CORRECTION (2026-08-18): this is NOT "pre-existing debt" as originally logged — it is a regression from merge `bcaa330` (2026-07-31), the same merge that silently dropped tuiOwnsBidi.** Counts by ref for `terminalInstance.ts`: `456f0ed` = 24 logger sites / 0 `console.*`; the other merge parent `45b6592` = 0 / 17; the merge result and `main` ever since = 0 / 17. Phase 79 DID convert this file; the merge resolution threw the conversion away. Restoring it is mechanical (re-apply the `456f0ed` version of those call sites onto current HEAD, keeping the newer clipboard/`diag_log` work) — deliberately NOT bundled into the tuiOwnsBidi restore to keep that diff reviewable. Raised to P2: Rule #9 says the unified logger is user-visible, so 17 blind sites is a support problem, not cosmetics.
- [ ] P1 | 2026-08-12 | app/src-tauri/src/rpc_server.rs (feed.push) | NOT VERIFIED LIVE: Stop-pulse fix. Hook feed items now derive workspace_id from pane_id, which is the prerequisite for the sidebar amber pulse (App.tsx activeHookWorkspaceIds) to fire on Stop. Needs a real ymux pane running Claude + a build of this branch to confirm the row actually pulses. The pane-BORDER pulse (OSC 9/99/777 from Claude Code's bell) is a separate path and was NOT touched — if that's the "blink" Yossi means, this fix won't address it.
- [ ] P3 | 2026-08-11 | app/src/terminalInstance.ts (18 sites) | Raw `console.*` throughout, against Rule #9 (frontend must use `createLogger`). This is how the clipboard-read failure stayed invisible: the only handler was `console.warn("terminal paste failed")`, which never reaches %APPDATA%\ymux\debug.log. The two clipboard paths were converted with the paste fix; the other ~16 (theme/font apply, WebGL fallback, pty_write, resetTerminal, OSC52) are untouched pre-existing debt.
- [ ] P3 | 2026-08-12 | app/src-tauri/src/rpc_server.rs (Decision::Auto) | Removed the per-Auto audit feed item (was flooding the feed with 300 "✓ matched no risky pattern" dots). If an audit trail of auto-approvals is ever wanted, it belongs in a dedicated filtered/collapsed view, NOT the live notification feed. debug.log already records each decision.
- [ ] P2 | 2026-08-03 | app/src-tauri/src/local_setup.rs:881 | WSL elevation detection is an English substring match (`low.contains("elevat") || "0x80070005"`) -> on localized Windows a UAC-declined `wsl --install` is misreported as a generic StepFailed. Also no ERROR_SUCCESS_REBOOT_REQUIRED (3010) / reboot-pending concept in inspect_wsl (:378-473).
- [ ] P2 | 2026-08-03 | app/src-tauri/src/fonts.rs:73 | Font catalog pins upstream tags+sha256; needs a periodic refresh check (JetBrainsMono v2.304, FiraCode 6.2, nerd-fonts v3.5.0, powerlevel10k-media master). An upstream re-release makes install fail with "checksum mismatch" — correct but opaque to the user.
- [ ] P2 | 2026-08-03 | app/src-tauri/src/fonts.rs:588 | MesloLGS NF assets are pinned to `master` on powerlevel10k-media, not a tag — the sha256 pin is what protects us, but a legit upstream update will break the entry until refreshed. Prefer a tagged source if one appears.
- [ ] P3 | 2026-08-03 | app/src-tauri/src/lib.rs:1604 | bidi_filter state is keyed by pane, so an SSH channel's stdout and stderr still share one escape-sequence state machine (the UTF-8 + OSC halves were split by the emit_data fix). A partial CSI/OSC on one stream can be completed by the other's bytes. Deliberate: a synthetic per-stream key would miss the per-pane smart_bidi toggle and silently leave stderr unfiltered. Only reachable via ExtendedData, rare on a PTY channel.
- [ ] P3 | 2026-08-03 | app/src-tauri/src/settings.rs:1662 | Mono/UI split is a name-substring heuristic; "Courier 10,12,15 (120)" bitmap .fon entries still reach the terminal picker and are known to measure badly (see terminalInstance.ts remeasureFont comment). Consider filtering bitmap .fon families out entirely.
- [ ] P3 | 2026-08-03 | app/src-tauri/src/settings.rs:1629 | Font enumeration shells out to PowerShell on every Settings open (~300ms). Fine at current call frequency; if the picker ever refreshes more often, read the registry via the winreg dep that fonts.rs now pulls in.
- [ ] P3 | 2026-08-03 | app/src-tauri/build.rs (resources) | A fresh git worktree can't `cargo build`: `app/src-tauri/resources/ymux-cli.exe` is gitignored, so the Tauri build script dies with "resource path doesn't exist". Has to be copied from the main checkout by hand. Either build it on demand or document the step.

- [ ] P2 | 2026-08-12 | app/src-tauri/src/diff_pane.rs:39 | The Diff pane is silently broken for every non-Local workspace. `fetch_diff` runs `git -C <cwd>` with `tokio::process::Command` on the WINDOWS host and `lookup_pane_context` (:74) never reads `ws.connection`. For an SSH or WSL workspace `cwd` is a Linux path, so `cwd.join(".git").exists()` at :40 is false and the pane shows "not a git repository" — it looks like the repo is broken rather than like the feature is local-only. Found while building the tickets transport layer, which now does resolve this correctly (see tickets.rs `resolve`). Fix: dispatch on connection the way tickets.rs does — `git -C … rev-parse` over the existing SSH exec channel, or the \\wsl.localhost share for WSL.
- [ ] P3 | 2026-08-12 | app/src-tauri/src/addons.rs:54 | `is_local_workspace` matches `None | Local` only, so a **WSL** workspace is classified as remote and routed down the SSH path, where it dies on "no active SSH session". Affects `insights_fetch` (:813), the docker action (:885) and hygiene (:916). WSL sits in a gap in the frontend predicates too (`types.ts:231-286`: `isRemoteConn` is ssh-only, `isLocalConn` is local-or-null). Fix: treat WSL as local for anything that only needs a filesystem/exec, which is what `local_setup::wsl_exec` already provides.
- [ ] P2 | 2026-08-13 | app/src/browserDevMode.ts (inspectScript → snapshot) | The element screenshot rendered as a fully TRANSPARENT png in the first live smoke — the modal preview was bare checkerboard. The captured element was a `<pre>`, which plausibly has no background of its own, so this may be correct behaviour. Unconfirmed either way. Repro: Dev Mode → right-click something with a visible background colour and text. If that is also blank, `foreignObject` is not drawing at all and the suspects are the `inlineStyles` pass or an `XMLSerializer` output the SVG parser rejects — both of which fail silently to a blank canvas today, with no way for the user to tell "the element really is transparent" from "the snapshot broke".
- [ ] P3 | 2026-08-13 | app/src-tauri/src/tickets.rs (`resolve`, ssh arm) | The tmux pane-cwd rung takes the FIRST session it finds for the workspace (`tmux_session_for_workspace`). With several panes sitting in different directories, the ticket follows whichever session the map happens to yield first, which is not deterministic across runs. Harmless while panes share a project — the observed case had three sessions all in `/home/runner/club` — but it should prefer the ACTIVE pane once that matters.

- [ ] P3 | 2026-08-19 | app/src/sidebar.css | SIDEBAR SURFACES STILL UNEXERCISED after the design pass. Not defects, just never run: (1) Settings -> Appearance -> UI size dragged 11pt..16pt -- the whole point of the px -> --w-fs-* sweep is that group headers, counts and the bottom action row now scale with it too, and only a live slider proves that; (2) drag-reorder of workspaces AND groups, whose CSS all moved file in b16537d; (3) the context menus, inline group rename and the colour picker; (4) 11 of the 13 presets -- app/dev/sidebar-fixture.html carries tokyo-night and modernist only, and themes-redesign.css:407 flattens radii by an explicit class list that any NEW sidebar class will be missing from; (5) prefers-reduced-motion. Cheapest sweep is one `npm run tauri dev` with the font slider and a drag, then flipping presets in Settings.
- [ ] P3 | 2026-08-28 | docs/RELEASING.md § 5 vs app/src-tauri/src/updater.rs (`struct Manifest`) | RELEASING.md's manifest template tells you to fill in `msi_size` and `nsis_size`, and the Rust `Manifest` struct declares neither field. serde has no `deny_unknown_fields` here so they are silently ignored — harmless, but every release cut copies two numbers nothing reads, and a future reader may assume the updater size-checks a download when it does not. Fix is one of: add the two fields to the struct and actually verify the downloaded length in `download_and_install_update`, or drop them from the template. Same file also carries `min_ymux_version` (renamed from `min_winmux_version` on 2026-08-28) which IS declared on `ManifestHook` but read nowhere.
